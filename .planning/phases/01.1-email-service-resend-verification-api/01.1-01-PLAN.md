---
phase: 01.1-email-service-resend-verification-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/utils/email.ts
  - apps/api/src/services/auth-service.ts
  - apps/api/src/services/invitation-service.ts
  - apps/api/tests/unit/email.test.ts
  - .env.example
autonomous: true
requirements:
  - EMAIL-02

must_haves:
  truths:
    - "sendEmail() 함수가 RESEND_API_KEY 있을 때 Resend SDK로 이메일을 발송한다"
    - "sendEmail() 함수가 RESEND_API_KEY 없을 때 logger.info()로 폴백한다"
    - "register() 호출 시 인증 이메일이 발송된다 (폴백 모드 포함)"
    - "forgotPassword() 호출 시 비밀번호 재설정 이메일이 발송된다"
    - "invitation create() 호출 시 초대 이메일이 발송된다"
  artifacts:
    - path: "apps/api/src/utils/email.ts"
      provides: "sendEmail() 헬퍼 + 3개 HTML 템플릿 함수"
      exports: ["sendEmail", "verificationEmailHtml", "passwordResetEmailHtml", "invitationEmailHtml"]
    - path: "apps/api/tests/unit/email.test.ts"
      provides: "sendEmail() 폴백 모드 + 템플릿 유닛 테스트"
    - path: ".env.example"
      provides: "환경변수 문서화"
      contains: "RESEND_API_KEY"
  key_links:
    - from: "apps/api/src/services/auth-service.ts"
      to: "apps/api/src/utils/email.ts"
      via: "import { sendEmail, verificationEmailHtml, passwordResetEmailHtml }"
      pattern: "sendEmail\\("
    - from: "apps/api/src/services/invitation-service.ts"
      to: "apps/api/src/utils/email.ts"
      via: "import { sendEmail, invitationEmailHtml }"
      pattern: "sendEmail\\("
---

<objective>
Resend SDK 설치, sendEmail() 헬퍼 + HTML 템플릿 생성, auth-service.ts와 invitation-service.ts의 logger.info() 3곳을 실제 이메일 발송으로 교체한다.

Purpose: 현재 콘솔에만 출력되는 인증/재설정/초대 이메일을 Resend를 통해 실제 발송하거나, API 키 미설정 시 기존 폴백 동작을 유지한다.
Output: email.ts 유틸리티, 3곳 교체 완료, 유닛 테스트, .env.example
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-email-service-resend-verification-api/01.1-CONTEXT.md
@.planning/phases/01.1-email-service-resend-verification-api/01.1-RESEARCH.md
@.planning/phases/01.1-email-service-resend-verification-api/01.1-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/api/src/utils/logger.ts:
```typescript
export const logger = {
  debug: createLogFn('debug'),
  info: createLogFn('info'),
  warn: createLogFn('warn'),
  error: createLogFn('error'),
} as const;
```

From apps/api/src/services/auth-service.ts:
```typescript
export function createAuthService(db: Db) {
  // register(): line 85 — logger.info(`Email verification link for ${user.email}: /api/v1/auth/verify-email?token=${emailVerifyToken}`);
  // forgotPassword(): line 263 — logger.info(`Password reset link for ${user.email}: /reset-password?token=${token}`);
  return { register, verifyEmail, login, refresh, logout, forgotPassword, resetPassword, changePassword };
}
```

From apps/api/src/services/invitation-service.ts:
```typescript
export function createInvitationService(db: Db) {
  // create(): line 45 — logger.info(`Invitation link for ${email}: /api/v1/invitations/${token}`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resend SDK 설치 + email.ts 유틸리티 생성 + 유닛 테스트</name>
  <files>apps/api/package.json, apps/api/src/utils/email.ts, apps/api/tests/unit/email.test.ts, .env.example</files>
  <read_first>
    - apps/api/package.json
    - apps/api/src/utils/logger.ts
    - .planning/phases/01.1-email-service-resend-verification-api/01.1-UI-SPEC.md
    - .planning/phases/01.1-email-service-resend-verification-api/01.1-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: sendEmail() 폴백 모드 — RESEND_API_KEY 미설정 시 logger.info() 호출, throw 하지 않음
    - Test 2: verificationEmailHtml(url) — 반환 HTML에 전달된 URL이 href로 포함됨, "이메일 인증하기" 버튼 텍스트 포함
    - Test 3: passwordResetEmailHtml(url) — 반환 HTML에 전달된 URL이 href로 포함됨, "비밀번호 재설정하기" 버튼 텍스트 포함
    - Test 4: invitationEmailHtml(url, workspaceName, inviterName) — 반환 HTML에 워크스페이스 이름과 초대자 이름 포함, "초대 수락하기" 버튼 텍스트 포함
    - Test 5: 모든 템플릿에 XSS 방어 — HTML 특수문자가 이스케이프됨 (사용자 입력인 workspaceName/inviterName에 `<script>` 주입 시 이스케이프)
  </behavior>
  <action>
1. Resend SDK 설치:
   ```bash
   pnpm --filter @markflow/api add resend
   ```

2. `apps/api/src/utils/email.ts` 생성:
   - Resend SDK 싱글턴: `const resend = process.env.RESEND_API_KEY ? new Resend(process.env.RESEND_API_KEY) : null;`
   - `EMAIL_FROM` 기본값: `process.env.EMAIL_FROM ?? 'MarkFlow <noreply@markflow.app>'`
   - `FRONTEND_URL` 기본값: `process.env.FRONTEND_URL ?? 'http://localhost:3000'`
   - `escapeHtml(str: string): string` 헬퍼 — `&`, `<`, `>`, `"`, `'` 이스케이프 (XSS 방어)
   - `SendEmailParams` 인터페이스: `{ to: string; subject: string; html: string }`
   - `sendEmail(params: SendEmailParams): Promise<void>` 함수:
     - `resend`가 null이면 `logger.info(`[EMAIL FALLBACK] To: ${params.to}, Subject: ${params.subject}`)` + `logger.info(`[EMAIL FALLBACK] Body: ${params.html}`)` 호출 후 return
     - `resend`가 있으면 `resend.emails.send({ from: EMAIL_FROM, to: [params.to], subject: params.subject, html: params.html })` 호출
     - `error` 필드 체크: `if (error) { logger.error(...); throw new Error(...); }` (try/catch 아님, D-04 pitfall #4)
     - 성공 시 `logger.info(`Email sent to ${params.to}`, { id: data?.id })` 로그
   - `verificationEmailHtml(verifyUrl: string): string` — UI-SPEC의 이메일 템플릿 비주얼 계약 따름:
     - `max-width: 480px; margin: 0 auto; padding: 40px 20px;`
     - heading: `이메일 인증` (20px, #1a1a1a, weight 600, margin-bottom 16px)
     - body: `MarkFlow에 가입해주셔서 감사합니다. 아래 버튼을 클릭하여 이메일 인증을 완료해주세요.` (14px, #4a4a4a, lh 1.6, margin-bottom 24px)
     - CTA: `이메일 인증하기` (padding 12px 24px, bg #1a56db, color #ffffff, border-radius 6px, 14px, weight 600, text-decoration none, display inline-block)
     - fine print: `이 링크는 24시간 동안 유효합니다.` (12px, #888, margin-top 24px)
     - font-family: `-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif` (시스템 스택)
   - `passwordResetEmailHtml(resetUrl: string): string` — 동일 레이아웃, 다른 카피:
     - heading: `비밀번호 재설정`
     - body: `비밀번호 재설정이 요청되었습니다. 아래 버튼을 클릭하여 새 비밀번호를 설정해주세요.`
     - CTA: `비밀번호 재설정하기`
     - fine print: `이 링크는 1시간 동안 유효합니다. 본인이 요청하지 않았다면 이 메일을 무시해주세요.`
   - `invitationEmailHtml(inviteUrl: string, workspaceName: string, inviterName: string): string` — 동일 레이아웃:
     - heading: `워크스페이스 초대`
     - body: `${escapeHtml(inviterName)}님이 ${escapeHtml(workspaceName)} 워크스페이스에 초대했습니다. 아래 버튼을 클릭하여 참여해주세요.`
     - CTA: `초대 수락하기`
     - fine print: `이 초대는 7일 동안 유효합니다.`
   - export 모두: `sendEmail`, `verificationEmailHtml`, `passwordResetEmailHtml`, `invitationEmailHtml`, `FRONTEND_URL` (서비스에서 URL 조합 시 사용)

3. `.env.example` 파일 생성 (D-08):
   ```
   # Database
   DATABASE_URL=postgresql://user:password@localhost:5432/markflow

   # Email (Resend) — optional, falls back to console logging if not set
   RESEND_API_KEY=re_xxxxxxxxxxxx
   EMAIL_FROM=MarkFlow <noreply@markflow.app>
   FRONTEND_URL=http://localhost:3000
   ```

4. 유닛 테스트 `apps/api/tests/unit/email.test.ts` — TDD: 테스트 먼저 작성 후 구현.
   - vi.mock으로 logger 모킹
   - RESEND_API_KEY 미설정 상태(기본)에서 sendEmail() 호출 시 logger.info가 `[EMAIL FALLBACK]` 포함 메시지로 호출되는지 검증
   - 각 템플릿 함수가 올바른 URL, 텍스트, 스타일을 포함하는지 문자열 검증
   - invitationEmailHtml에 `<script>alert(1)</script>` 주입 시 `&lt;script&gt;` 로 이스케이프되는지 검증
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/api test -- tests/unit/email.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/utils/email.ts 파일 존재
    - email.ts에 `export async function sendEmail(` 포함
    - email.ts에 `export function verificationEmailHtml(` 포함
    - email.ts에 `export function passwordResetEmailHtml(` 포함
    - email.ts에 `export function invitationEmailHtml(` 포함
    - email.ts에 `new Resend(` 포함
    - email.ts에 `logger.info` 포함 (폴백 모드)
    - email.ts에 `escapeHtml` 함수 포함
    - .env.example에 `RESEND_API_KEY=` 포함
    - .env.example에 `EMAIL_FROM=` 포함
    - .env.example에 `FRONTEND_URL=` 포함
    - apps/api/tests/unit/email.test.ts 존재하고 테스트 통과
    - apps/api/package.json에 `"resend"` 의존성 포함
  </acceptance_criteria>
  <done>sendEmail() 헬퍼가 폴백 모드에서 logger.info()를 호출하고, 3개 HTML 템플릿 함수가 UI-SPEC 계약에 맞는 HTML을 반환하며, 유닛 테스트가 모두 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: auth-service.ts와 invitation-service.ts의 logger.info() 3곳을 sendEmail()로 교체</name>
  <files>apps/api/src/services/auth-service.ts, apps/api/src/services/invitation-service.ts</files>
  <read_first>
    - apps/api/src/services/auth-service.ts
    - apps/api/src/services/invitation-service.ts
    - apps/api/src/utils/email.ts
  </read_first>
  <action>
1. **auth-service.ts** 상단에 import 추가:
   ```typescript
   import { sendEmail, verificationEmailHtml, passwordResetEmailHtml, FRONTEND_URL } from '../utils/email.js';
   ```

2. **register() 함수** (현재 85행) — 교체:
   ```typescript
   // BEFORE (삭제):
   logger.info(`Email verification link for ${user.email}: /api/v1/auth/verify-email?token=${emailVerifyToken}`);

   // AFTER (교체):
   const verifyUrl = `${FRONTEND_URL}/verify-email?token=${emailVerifyToken}`;
   await sendEmail({
     to: user.email,
     subject: 'MarkFlow 이메일 인증',
     html: verificationEmailHtml(verifyUrl),
   });
   ```

3. **forgotPassword() 함수** (현재 263행) — 교체:
   ```typescript
   // BEFORE (삭제):
   logger.info(`Password reset link for ${user.email}: /reset-password?token=${token}`);

   // AFTER (교체):
   const resetUrl = `${FRONTEND_URL}/reset-password?token=${token}`;
   await sendEmail({
     to: user.email,
     subject: 'MarkFlow 비밀번호 재설정',
     html: passwordResetEmailHtml(resetUrl),
   });
   ```

4. **invitation-service.ts** 상단에 import 추가:
   ```typescript
   import { sendEmail, invitationEmailHtml, FRONTEND_URL } from '../utils/email.js';
   ```

5. **invitation-service.ts create() 함수** (현재 45행) — 교체. 초대 이메일에 workspaceName과 inviterName이 필요하지만, create()의 파라미터는 `(workspaceId, inviterId, email, role)`이므로 DB 조회가 필요하다. create() 내부에서 workspace와 inviter 정보를 조회:
   ```typescript
   // logger.info() 삭제 후, 그 위치에 다음 추가:
   // workspace 이름과 inviter 이름 조회
   const [workspace] = await db
     .select({ name: workspaces.name })
     .from(workspaces)
     .where(eq(workspaces.id, Number(workspaceId)))
     .limit(1);
   const [inviter] = await db
     .select({ name: users.name })
     .from(users)
     .where(eq(users.id, Number(inviterId)))
     .limit(1);

   const inviteUrl = `${FRONTEND_URL}/invitations/${token}`;
   await sendEmail({
     to: email,
     subject: 'MarkFlow 워크스페이스 초대',
     html: invitationEmailHtml(
       inviteUrl,
       workspace?.name ?? 'MarkFlow 워크스페이스',
       inviter?.name ?? '팀원',
     ),
   });
   ```
   주의: `workspaces`와 `users`는 이미 invitation-service.ts 상단에 import되어 있다.

6. 기존 테스트 실행하여 회귀 없음 확인: `pnpm --filter @markflow/api test`
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/api test</automated>
  </verify>
  <acceptance_criteria>
    - auth-service.ts에 `import { sendEmail, verificationEmailHtml, passwordResetEmailHtml, FRONTEND_URL } from '../utils/email.js'` 포함
    - auth-service.ts에 `logger.info(\`Email verification link` 문자열 없음 (교체됨)
    - auth-service.ts에 `logger.info(\`Password reset link` 문자열 없음 (교체됨)
    - auth-service.ts에 `await sendEmail({` 2곳 존재
    - invitation-service.ts에 `import { sendEmail, invitationEmailHtml, FRONTEND_URL } from '../utils/email.js'` 포함
    - invitation-service.ts에 `logger.info(\`Invitation link` 문자열 없음 (교체됨)
    - invitation-service.ts에 `await sendEmail({` 1곳 존재
    - 기존 전체 테스트 스위트 통과 (`pnpm --filter @markflow/api test` exit 0)
  </acceptance_criteria>
  <done>auth-service.ts의 register(), forgotPassword()와 invitation-service.ts의 create()에서 logger.info() 대신 sendEmail()을 호출하며, 폴백 모드에서 기존 테스트가 모두 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Resend API | API 키로 인증된 외부 서비스 호출 |
| User input → Email template | 초대 이메일에 workspaceName/inviterName 삽입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Information Disclosure | sendEmail() 로그 | mitigate | 폴백 모드에서 logger.info()로 HTML 본문 출력 시 프로덕션 로그에 민감 정보 노출 가능. NODE_ENV=production에서 RESEND_API_KEY 미설정 시 logger.warn() 경고 추가 |
| T-01.1-02 | Tampering | invitationEmailHtml | mitigate | workspaceName/inviterName에 HTML 주입 가능. escapeHtml() 함수로 &, <, >, ", ' 이스케이프 처리 |
| T-01.1-03 | Spoofing | RESEND_API_KEY | accept | API 키 유출 시 임의 이메일 발송 가능. 환경변수로만 관리, .env.example에 실제 키 미포함 (표준 관행) |
| T-01.1-04 | Denial of Service | sendEmail() | accept | Resend API 장애 시 이메일 미발송. MVP에서는 재시도 미구현, 에러 로그만 기록 |
</threat_model>

<verification>
1. `pnpm --filter @markflow/api test -- tests/unit/email.test.ts` — 유닛 테스트 통과
2. `pnpm --filter @markflow/api test` — 전체 테스트 스위트 통과 (회귀 없음)
3. auth-service.ts에서 `logger.info.*Email verification link` 패턴 없음
4. auth-service.ts에서 `logger.info.*Password reset link` 패턴 없음
5. invitation-service.ts에서 `logger.info.*Invitation link` 패턴 없음
</verification>

<success_criteria>
- sendEmail() 헬퍼가 Resend SDK 또는 logger.info() 폴백으로 동작한다
- 3개 HTML 이메일 템플릿이 UI-SPEC 비주얼 계약을 따른다
- auth-service.ts와 invitation-service.ts의 logger.info() 3곳이 sendEmail()로 교체되었다
- .env.example에 RESEND_API_KEY, EMAIL_FROM, FRONTEND_URL이 문서화되었다
- 기존 테스트 스위트가 모두 통과한다 (폴백 모드)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-email-service-resend-verification-api/01.1-01-SUMMARY.md`
</output>
