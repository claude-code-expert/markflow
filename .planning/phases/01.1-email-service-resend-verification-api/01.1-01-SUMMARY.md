---
phase: 01.1-email-service-resend-verification-api
plan: 01
subsystem: api
tags: [resend, email, html-template, xss-prevention]

# Dependency graph
requires:
  - phase: 01-security-auth-hardening
    provides: auth-service.ts, invitation-service.ts with logger.info() email stubs
provides:
  - sendEmail() helper with Resend SDK + logger.info() fallback
  - 3 HTML email template functions (verification, password reset, invitation)
  - escapeHtml() XSS prevention for user input in templates
  - FRONTEND_URL export for email link construction
affects: [01.1-02, email-service, auth-flow, invitation-flow]

# Tech tracking
tech-stack:
  added: [resend]
  patterns: [sendEmail-fallback-pattern, inline-css-email-templates]

key-files:
  created:
    - apps/api/src/utils/email.ts
    - apps/api/tests/unit/email.test.ts
  modified:
    - apps/api/src/services/auth-service.ts
    - apps/api/src/services/invitation-service.ts
    - apps/api/package.json

key-decisions:
  - "Resend SDK singleton at module level with null fallback when RESEND_API_KEY is absent"
  - "escapeHtml() for XSS prevention on user-supplied workspaceName and inviterName in invitation template"
  - "invitation-service.ts queries workspace and inviter names via separate DB queries for email template"

patterns-established:
  - "sendEmail fallback: resend instance is null when no API key, logger.info with [EMAIL FALLBACK] prefix"
  - "HTML email template: inline CSS, system font stack, max-width 480px container, #1a56db CTA button"

requirements-completed: [EMAIL-02]

# Metrics
duration: 9min
completed: 2026-04-13
---

# Phase 01.1 Plan 01: Email Service Summary

**Resend SDK 통합 및 sendEmail() 헬퍼 생성, auth-service/invitation-service의 logger.info() 3곳을 실제 이메일 발송으로 교체**

## Performance

- **Duration:** 9 min
- **Started:** 2026-04-13T04:57:03Z
- **Completed:** 2026-04-13T05:06:32Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- sendEmail() 헬퍼 함수 구현 -- RESEND_API_KEY 유무에 따라 Resend SDK 또는 logger.info() 폴백
- 3개 HTML 이메일 템플릿 함수 (인증, 비밀번호 재설정, 초대) -- UI-SPEC 비주얼 계약 준수
- XSS 방어를 위한 escapeHtml() 적용 (workspaceName, inviterName)
- auth-service.ts register(), forgotPassword() 및 invitation-service.ts create()의 logger.info() 3곳 교체
- 10개 유닛 테스트 모두 통과

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): email utility failing tests** - `dcd2437` (test)
2. **Task 1 (GREEN): email utility + Resend SDK + templates** - `c64014d` (feat)
3. **Task 1: lockfile update** - `a1dd60d` (chore)
4. **Task 2: replace logger.info with sendEmail in services** - `ada4f8d` (feat)

## Files Created/Modified
- `apps/api/src/utils/email.ts` - sendEmail() 헬퍼, 3개 HTML 템플릿 함수, escapeHtml(), FRONTEND_URL export
- `apps/api/tests/unit/email.test.ts` - 10개 유닛 테스트 (폴백 모드, 템플릿 내용, XSS 방어)
- `apps/api/src/services/auth-service.ts` - register()와 forgotPassword()에서 sendEmail() 호출로 교체
- `apps/api/src/services/invitation-service.ts` - create()에서 workspace/inviter 이름 DB 조회 후 sendEmail() 호출로 교체
- `apps/api/package.json` - resend 의존성 추가

## Decisions Made
- Resend SDK를 모듈 레벨 싱글턴으로 초기화 (`const resend = RESEND_API_KEY ? new Resend(RESEND_API_KEY) : null`)
- invitation-service.ts create()에서 workspaceName/inviterName을 위해 별도 DB SELECT 쿼리 2개 추가 (기존 create 메서드 시그니처 유지)
- 유닛 테스트에서 vi.resetModules() + vi.doMock() 패턴 사용 (setup.ts의 buildApp()이 email.ts를 먼저 로드하는 문제 해결)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] .env.example 파일 생성 불가**
- **Found during:** Task 1
- **Issue:** 프로젝트 git hook이 `.env*` 패턴의 모든 파일 생성/수정을 차단
- **Fix:** .env.example 생성을 건너뜀. 환경변수(RESEND_API_KEY, EMAIL_FROM, FRONTEND_URL)는 email.ts 소스코드 내 기본값과 주석으로 문서화됨
- **Files modified:** 없음 (차단됨)
- **Verification:** email.ts에 기본값이 명시되어 있어 개발자가 필요한 환경변수를 파악 가능

**2. [Rule 1 - Bug] 테스트 모킹 패턴 수정**
- **Found during:** Task 1 (TDD GREEN)
- **Issue:** setup.ts의 buildApp()이 email.ts를 먼저 로드하여 vi.mock()이 logger를 가로채지 못함
- **Fix:** vi.resetModules() + vi.doMock() 패턴으로 sendEmail 폴백 테스트 수정
- **Files modified:** apps/api/tests/unit/email.test.ts
- **Verification:** 10개 테스트 모두 통과

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** .env.example 미생성은 기능에 영향 없음 (코드 내 기본값 존재). 테스트 모킹 수정은 테스트 정확성을 위해 필수.

## Threat Model Compliance

| Threat ID | Disposition | Status |
|-----------|-------------|--------|
| T-01.1-01 | mitigate | PARTIAL - 폴백 모드에서 logger.info()로 HTML 본문 출력. 프로덕션 경고는 미구현 (Plan 02 scope) |
| T-01.1-02 | mitigate | DONE - escapeHtml()로 workspaceName/inviterName XSS 방어 |
| T-01.1-03 | accept | DONE - API 키는 환경변수로만 관리 |
| T-01.1-04 | accept | DONE - Resend 에러 시 throw + logger.error 로그 |

## Issues Encountered
- pnpm-lock.yaml이 resend 설치 후 변경되어 별도 커밋 필요
- @markflow/db 패키지 빌드가 필요했음 (worktree에서 초기 실행 시)

## Known Stubs
없음 -- 모든 기능이 완전히 구현됨

## Next Phase Readiness
- email.ts 유틸리티가 준비되어 Plan 02 (resend-verification API)에서 재사용 가능
- sendEmail(), verificationEmailHtml(), FRONTEND_URL이 export되어 resendVerification 서비스 로직에서 바로 사용 가능

---
*Phase: 01.1-email-service-resend-verification-api*
*Completed: 2026-04-13*
