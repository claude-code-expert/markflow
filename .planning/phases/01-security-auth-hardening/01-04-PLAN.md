---
phase: 01-security-auth-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - apps/web/components/password-change-modal.tsx
  - apps/web/app/(app)/[workspaceSlug]/settings/page.tsx
autonomous: false
requirements:
  - AUTH-01
must_haves:
  truths:
    - "사용자가 설정 페이지에서 비밀번호 변경 모달을 열 수 있다"
    - "모달에서 현재 비밀번호, 새 비밀번호, 확인 필드를 입력할 수 있다"
    - "새 비밀번호 입력 시 실시간으로 hint pills(8자, 영문, 숫자, 특수문자)가 표시된다"
    - "변경 성공 시 토스트가 표시되고 모달이 닫힌다"
    - "API 에러(잘못된 비밀번호, 잠금) 시 적절한 알림이 표시된다"
  artifacts:
    - path: "apps/web/components/password-change-modal.tsx"
      provides: "Password change modal component"
      exports: ["PasswordChangeModal"]
      min_lines: 100
    - path: "apps/web/app/(app)/[workspaceSlug]/settings/page.tsx"
      provides: "Settings page with password change trigger"
      contains: "PasswordChangeModal"
  key_links:
    - from: "apps/web/components/password-change-modal.tsx"
      to: "PUT /api/v1/users/me/password"
      via: "apiFetch or fetch call"
      pattern: "users/me/password"
    - from: "apps/web/components/password-change-modal.tsx"
      to: "toast-store.ts"
      via: "success toast notification"
      pattern: "toast"
---

<objective>
비밀번호 변경 모달 UI를 구현하여 사용자가 설정 페이지에서 비밀번호를 변경할 수 있게 한다. UI-SPEC에 정의된 디자인 계약을 정확히 따른다.

Purpose: AUTH-01의 프론트엔드 완성 -- 백엔드 API(Plan 03)와 연동하여 전체 비밀번호 변경 플로우를 제공한다.
Output: PasswordChangeModal 컴포넌트, 설정 페이지 연동
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-hardening/01-CONTEXT.md
@.planning/phases/01-security-auth-hardening/01-UI-SPEC.md
@.planning/phases/01-security-auth-hardening/01-03-SUMMARY.md

<interfaces>
<!-- UI-SPEC에 정의된 스타일 토큰 -->
From 01-UI-SPEC.md (Style Pattern):
```typescript
// Card (modal)
{ maxWidth: 440, background: 'var(--surface)', borderRadius: 'var(--radius-xl)', boxShadow: 'var(--shadow-lg)', padding: 32 }

// Field group
{ marginBottom: 16, textAlign: 'left' as const }

// Label
{ display: 'block', fontSize: 13, fontWeight: 600, color: 'var(--text-2)', marginBottom: 8 }

// Input
{ width: '100%', borderWidth: '1.5px', borderStyle: 'solid', borderColor: 'var(--border)', borderRadius: 'var(--radius-sm)', padding: '8px 12px', fontSize: 14, color: 'var(--text)', background: 'var(--surface)' }

// Input focus
{ borderColor: 'var(--accent)', boxShadow: '0 0 0 3px rgba(26,86,219,.1)' }

// Input error
{ borderColor: 'var(--red)' }

// Primary button (focal point)
{ width: '100%', padding: 12, fontSize: 14, fontWeight: 600, color: '#fff', background: 'var(--accent)', border: 'none', borderRadius: 'var(--radius)' }

// Primary button disabled
{ opacity: 0.55, cursor: 'not-allowed' }

// Alert error
{ borderRadius: 'var(--radius-sm)', padding: '8px 16px', fontSize: 13, lineHeight: 1.5, background: 'var(--red-lt)', border: '1px solid var(--red)', color: 'var(--red)' }

// Alert warning (lockout)
{ borderRadius: 'var(--radius-sm)', padding: '8px 16px', fontSize: 13, lineHeight: 1.5, background: 'var(--amber-lt)', border: '1px solid var(--amber)', color: 'var(--amber)' }
```

<!-- 재사용할 기존 컴포넌트 패턴 -->
From 01-UI-SPEC.md (Existing Components to Reuse):
```
Toast: components/toast-provider.tsx + stores/toast-store.ts
Error alert: login/page.tsx styles.alertError
Warning alert: login/page.tsx styles.alertWarning
Password hint pills: reset-password/page.tsx hintPill + getPasswordChecks()
Field error: reset-password/page.tsx styles.fieldError
Countdown badge: login/page.tsx styles.countdownBadge
```

<!-- API 응답 형식 (Plan 03에서 구현) -->
PUT /api/v1/users/me/password:
```typescript
// Request
{ currentPassword: string; newPassword: string }

// Success (200)
{ accessToken: string; refreshToken: string }

// Error (401)
{ error: { code: 'INVALID_CREDENTIALS' | 'ACCOUNT_LOCKED', message: string } }

// Error (400)
{ error: { code: 'INVALID_PASSWORD' | 'MISSING_FIELDS', message: string } }
```

<!-- Copywriting Contract -->
From 01-UI-SPEC.md:
```
Heading: "비밀번호 변경"
Description: "보안을 위해 현재 비밀번호를 확인한 후 변경합니다."
Primary CTA: "비밀번호 변경"
CTA loading: "변경 중..."
Cancel: "취소"
Success toast: "비밀번호가 변경되었습니다"
Session notice: "다른 기기의 로그인이 모두 해제되었습니다."
Error wrong pw: "현재 비밀번호가 올바르지 않습니다."
Error mismatch: "새 비밀번호가 일치하지 않습니다."
Error locked: "계정이 잠겼습니다. {N}분 후에 다시 시도해주세요."
Error rate limit: "시도가 너무 많습니다. 잠시 후 다시 시도해주세요."
Error network: "네트워크 오류가 발생했습니다. 다시 시도해주세요."
Hint pills: "8자 이상", "영문 포함", "숫자 포함", "특수문자 포함"
Labels: "현재 비밀번호", "새 비밀번호", "새 비밀번호 확인"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PasswordChangeModal 컴포넌트 구현</name>
  <files>apps/web/components/password-change-modal.tsx</files>
  <read_first>
    - .planning/phases/01-security-auth-hardening/01-UI-SPEC.md (전체 -- 디자인 계약)
    - apps/web/app/(auth)/reset-password/page.tsx (hintPill, getPasswordChecks() 패턴 -- 그대로 복사)
    - apps/web/app/(auth)/login/page.tsx (alertError, alertWarning, countdownBadge 스타일 참조)
    - apps/web/components/toast-provider.tsx (토스트 사용 패턴)
    - apps/web/stores/toast-store.ts (toast.success() 호출법)
    - apps/web/components/profile-edit-modal.tsx (모달 컴포넌트 패턴 -- 존재하면 참조)
    - apps/web/lib/api-fetch.ts (apiFetch 유틸리티 또는 동일 역할의 HTTP 클라이언트)
    - apps/web/stores/auth-store.ts (인증 상태 관리 -- 토큰 갱신 방법)
  </read_first>
  <action>
    **PasswordChangeModal 컴포넌트 생성 -- UI-SPEC 디자인 계약 준수:**

    ```typescript
    'use client';

    import { useState, type CSSProperties, type FormEvent } from 'react';
    ```

    **Props 인터페이스:**
    ```typescript
    interface PasswordChangeModalProps {
      isOpen: boolean;
      onClose: () => void;
    }
    ```

    **State:**
    - `currentPassword`, `newPassword`, `confirmPassword` (string)
    - `isSubmitting` (boolean)
    - `apiError` (null | { code: string; message: string })
    - `fieldErrors` (Record<string, string>)

    **getPasswordChecks 함수 (reset-password/page.tsx에서 복사):**
    ```typescript
    function getPasswordChecks(password: string) {
      return [
        { label: '8자 이상', passed: password.length >= 8 },
        { label: '영문 포함', passed: /[a-zA-Z]/.test(password) },
        { label: '숫자 포함', passed: /\d/.test(password) },
        { label: '특수문자 포함', passed: /[!@#$%^&*()_+\-=[\]{};':"\\|,.<>/?]/.test(password) },
      ];
    }
    ```

    **Client-side validation (submit 전):**
    1. currentPassword: required (non-empty)
    2. newPassword: 4개 checks 모두 passed
    3. confirmPassword: newPassword와 일치

    **Submit flow (UI-SPEC Interaction Contract 참조):**
    1. Client validation -> 실패 시 fieldErrors 설정, 제출 중단
    2. `isSubmitting = true`, 버튼 비활성화
    3. `PUT /api/v1/users/me/password` 호출 (apiFetch 또는 fetch)
    4. 성공 (200):
       a. 응답의 accessToken, refreshToken으로 auth store 업데이트
       b. `onClose()` 호출 (모달 닫기)
       c. toast.success('비밀번호가 변경되었습니다') + 세션 무효화 안내
    5. 에러:
       - `INVALID_CREDENTIALS` -> apiError 설정 (red alert)
       - `ACCOUNT_LOCKED` -> apiError 설정 (amber alert with countdown)
       - `RATE_LIMITED` -> apiError 설정 (amber alert)
       - `INVALID_PASSWORD` -> apiError 설정 (server message passthrough)
       - Network error -> apiError 설정 (generic message)
    6. `isSubmitting = false`

    **스타일 -- 모든 값은 UI-SPEC의 Style Pattern에서 직접 복사:**
    - 모달 overlay: `position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.4)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 50`
    - 모달 카드: UI-SPEC Card 스타일
    - Heading: `fontSize: 18, fontWeight: 600, fontFamily: 'var(--font-heading)', marginBottom: 8`
    - Description: `fontSize: 14, color: 'var(--text-3)', marginBottom: 24, lineHeight: 1.6`
    - Input, Label, Button, Alert 모두 UI-SPEC 스타일 그대로 적용
    - Hint pills: `fontSize: 12, padding: '4px 8px', borderRadius: 9999`, passed -> green bg, not passed -> gray bg

    **Keyboard & Accessibility (UI-SPEC 참조):**
    - `<form>` 태그 사용 (Enter로 submit)
    - Escape 키로 모달 닫기 (`useEffect` 키보드 이벤트)
    - `role="alert"` on error/warning alerts
    - `aria-label` on close button
    - autocomplete 속성: `current-password`, `new-password`, `new-password`

    **Tab order:** 현재 비밀번호 -> 새 비밀번호 -> 새 비밀번호 확인 -> 취소 -> 비밀번호 변경

    **named export:** `export function PasswordChangeModal({ isOpen, onClose }: PasswordChangeModalProps)`
    - default export 금지 (프로젝트 컨벤션)
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/markflow && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/password-change-modal.tsx 파일이 존재
    - 파일에 `export function PasswordChangeModal` (named export, default export 아님)
    - 파일에 `users/me/password` URL 문자열이 존재 (API 호출)
    - 파일에 `getPasswordChecks` 함수가 존재 (hint pills)
    - 파일에 `INVALID_CREDENTIALS` 에러 코드 처리가 존재
    - 파일에 `ACCOUNT_LOCKED` 에러 코드 처리가 존재
    - 파일에 `autocomplete` 속성이 3개 input에 설정됨 (current-password, new-password x2)
    - 파일에 `role="alert"` 속성이 에러/경고 알림에 존재
    - 파일에 `비밀번호 변경` 텍스트가 존재 (Heading + CTA)
    - 파일에 `변경 중...` 텍스트가 존재 (loading state)
    - 파일에 `비밀번호가 변경되었습니다` 텍스트가 존재 (success toast)
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>PasswordChangeModal이 UI-SPEC 디자인 계약대로 구현되고, 모든 상태(default, validating, error, submitting, success, locked)를 처리하며, TypeScript 에러 없이 컴파일된다</done>
</task>

<task type="auto">
  <name>Task 2: 설정 페이지에 비밀번호 변경 버튼 + 모달 연결</name>
  <files>apps/web/app/(app)/[workspaceSlug]/settings/page.tsx</files>
  <read_first>
    - apps/web/app/(app)/[workspaceSlug]/settings/page.tsx (현재 설정 페이지 구조)
    - apps/web/components/password-change-modal.tsx (Task 1에서 생성)
    - apps/web/components/profile-edit-modal.tsx (모달 트리거 패턴 참조, 존재하면)
  </read_first>
  <action>
    **설정 페이지에 비밀번호 변경 섹션 추가:**

    1. `PasswordChangeModal` import 추가:
    ```typescript
    import { PasswordChangeModal } from '@/components/password-change-modal';
    ```

    2. State 추가:
    ```typescript
    const [isPasswordModalOpen, setIsPasswordModalOpen] = useState(false);
    ```

    3. 설정 페이지 내 적절한 위치(프로필 섹션 아래 또는 보안 섹션)에 "비밀번호 변경" 버튼 추가:
    ```typescript
    <div style={{ marginTop: 24 }}>
      <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 12, color: 'var(--text)' }}>보안</h3>
      <button
        type="button"
        onClick={() => setIsPasswordModalOpen(true)}
        style={{
          padding: '8px 16px',
          fontSize: 14,
          fontWeight: 600,
          color: 'var(--text)',
          background: 'var(--surface)',
          border: '1.5px solid var(--border)',
          borderRadius: 'var(--radius)',
          cursor: 'pointer',
        }}
      >
        비밀번호 변경
      </button>
    </div>
    ```

    4. PasswordChangeModal 렌더링:
    ```typescript
    <PasswordChangeModal
      isOpen={isPasswordModalOpen}
      onClose={() => setIsPasswordModalOpen(false)}
    />
    ```

    기존 설정 페이지의 스타일 패턴과 일관되게 구현. 기존 코드를 수정하되 손상하지 않음.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/markflow && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - settings/page.tsx에 `PasswordChangeModal` import가 존재
    - settings/page.tsx에 `isPasswordModalOpen` state가 존재
    - settings/page.tsx에 `비밀번호 변경` 텍스트가 포함된 버튼이 존재
    - settings/page.tsx에 `<PasswordChangeModal` JSX가 존재
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>설정 페이지에서 "비밀번호 변경" 버튼 클릭 시 모달이 열리고, 모달 닫기 시 정상적으로 닫힌다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 비밀번호 변경 플로우 시각적 검증</name>
  <files>apps/web/components/password-change-modal.tsx, apps/web/app/(app)/[workspaceSlug]/settings/page.tsx</files>
  <action>사용자가 전체 비밀번호 변경 플로우를 직접 테스트하여 UI와 기능을 검증한다.</action>
  <what-built>비밀번호 변경 전체 플로우: API 엔드포인트 (Plan 03) + UI 모달 (이 플랜)</what-built>
  <how-to-verify>
    1. `pnpm dev`로 개발 서버 시작
    2. 로그인 후 워크스페이스 설정 페이지로 이동
    3. "비밀번호 변경" 버튼이 보이는지 확인
    4. 버튼 클릭 -> 모달이 열리는지 확인
    5. 모달에 3개 필드(현재 비밀번호, 새 비밀번호, 새 비밀번호 확인)가 있는지 확인
    6. 새 비밀번호 입력 시 hint pills(8자 이상, 영문 포함, 숫자 포함, 특수문자 포함)가 실시간으로 변하는지 확인
    7. 잘못된 현재 비밀번호 입력 -> "현재 비밀번호가 올바르지 않습니다." 빨간 알림 확인
    8. 올바른 비밀번호로 변경 -> 토스트 "비밀번호가 변경되었습니다" 확인, 모달 닫힘 확인
    9. 변경된 비밀번호로 재로그인 가능한지 확인
    10. Escape 키로 모달 닫기 확인
  </how-to-verify>
  <verify>사용자가 10단계 검증을 완료하고 "approved"를 입력한다</verify>
  <done>사용자가 비밀번호 변경 플로우 전체를 시각적으로 검증하고 승인한다</done>
  <resume-signal>"approved"를 입력하거나 수정이 필요한 부분을 설명해주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Client validation | 클라이언트 측 유효성 검사 (보조 -- 서버 검증이 권위) |
| Client -> API (PUT /me/password) | 인증된 요청으로 비밀번호 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Information Disclosure | Password field | mitigate | type="password" 사용, autocomplete 속성으로 브라우저 자동완성 제어 |
| T-01-18 | Tampering | Client validation bypass | accept | 클라이언트 검증은 UX용이며 서버에서 최종 검증 (Plan 03) |
| T-01-19 | Denial of Service | Error flooding | mitigate | isSubmitting으로 중복 제출 방지, 서버 rate limit (Plan 03) |
</threat_model>

<verification>
1. 설정 페이지에 "비밀번호 변경" 버튼 존재
2. 버튼 클릭 시 모달 열림
3. 3개 필드 + hint pills + submit 버튼 확인
4. 올바른 비밀번호로 변경 -> 성공 토스트 + 모달 닫힘
5. 잘못된 비밀번호 -> 에러 알림 표시
6. TypeScript 컴파일 에러 없음
</verification>

<success_criteria>
- PasswordChangeModal이 UI-SPEC 디자인 계약대로 구현됨
- 설정 페이지에서 모달을 열고 닫을 수 있음
- 비밀번호 변경 성공/실패 플로우가 모두 동작함
- 실시간 hint pills가 표시됨
- 사용자가 visual verification으로 승인함
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-hardening/01-04-SUMMARY.md`
</output>
