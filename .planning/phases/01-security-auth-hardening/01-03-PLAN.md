---
phase: 01-security-auth-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/users.ts
  - apps/api/src/services/auth-service.ts
  - apps/api/tests/integration/password-change.test.ts
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "로그인한 사용자가 현재 비밀번호 확인 후 새 비밀번호로 변경할 수 있다"
    - "비밀번호 변경 성공 시 현재 세션을 제외한 모든 refresh token이 무효화된다"
    - "비밀번호 변경 성공 응답에 새 access + refresh token이 포함된다"
    - "비밀번호 변경 5회 실패 시 15분 계정 잠금이 적용된다"
  artifacts:
    - path: "apps/api/src/routes/users.ts"
      provides: "PUT /me/password endpoint"
      contains: "me/password"
    - path: "apps/api/src/services/auth-service.ts"
      provides: "changePassword method with transaction"
      contains: "changePassword"
    - path: "apps/api/tests/integration/password-change.test.ts"
      provides: "Password change integration tests"
      min_lines: 100
  key_links:
    - from: "apps/api/src/routes/users.ts"
      to: "auth-service.changePassword"
      via: "PUT /me/password handler calls changePassword()"
      pattern: "changePassword"
    - from: "apps/api/src/services/auth-service.ts"
      to: "refreshTokens table"
      via: "db.transaction DELETE + INSERT"
      pattern: "db\\.transaction"
---

<objective>
비밀번호 변경 API를 구현하여 인증된 사용자가 현재 비밀번호 확인 후 새 비밀번호로 변경할 수 있게 한다. 변경 성공 시 전체 세션을 무효화하고 새 토큰을 발급한다.

Purpose: AUTH-01 요구사항 충족 — 계정 보안의 기본인 비밀번호 변경 기능과 세션 관리를 완성한다.
Output: PUT /me/password API, changePassword 서비스, 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-hardening/01-CONTEXT.md
@.planning/phases/01-security-auth-hardening/01-RESEARCH.md

<interfaces>
<!-- 재사용할 유틸리티 -->
From apps/api/src/utils/password.ts:
```typescript
export async function hashPassword(password: string): Promise<string>;
export async function comparePassword(password: string, hash: string): Promise<boolean>;
export function validatePassword(password: string): { valid: boolean; message?: string };
// PASSWORD_MIN_LENGTH = 8
// PASSWORD_REGEX = /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[!@#$%^&*()_+\-=[\]{};':"\\|,.<>/?]).+$/
```

From apps/api/src/utils/jwt.ts:
```typescript
export function signTokenPair(payload: TokenPayload, rememberMe?: boolean): TokenPair;
export function getRefreshTokenExpiry(rememberMe?: boolean): Date;
// TokenPayload = { userId: string; email: string }
// TokenPair = { accessToken: string; refreshToken: string }
```

From apps/api/src/utils/errors.ts:
```typescript
export class AppError extends Error {
  readonly code: string; readonly statusCode: number; readonly details?: Record<string, unknown>;
}
export function badRequest(code: string, message: string, details?: Record<string, unknown>): AppError;
export function unauthorized(message?: string): AppError;
```

<!-- auth-service 기존 패턴 -->
From apps/api/src/services/auth-service.ts:
```typescript
export function createAuthService(db: Db) {
  // 반환: { register, verifyEmail, login, refresh, logout }
  // login()에 비밀번호 검증 + 실패 카운트 + 잠금 로직 존재 (line 119-195)
  // hashToken(): crypto.createHash('sha256').update(token).digest('hex')
}
```

<!-- users 라우트 기존 패턴 -->
From apps/api/src/routes/users.ts:
```typescript
export async function usersRoutes(app: FastifyInstance, opts: UsersRoutesOptions) {
  app.addHook('preHandler', authMiddleware); // 모든 라우트 인증 필수
  // GET /me, PATCH /me 존재
}
```

<!-- auth 라우트 rate limit 패턴 -->
From apps/api/src/routes/auth.ts:
```typescript
function authRateLimit(max = 10) {
  return { config: { rateLimit: { max, timeWindow: '15 minutes', keyGenerator: (req) => req.ip } } };
}
```

<!-- DB 스키마 참조 -->
From packages/db/src/schema/users.ts:
```typescript
// users 테이블에 loginFailCount (integer, default 0), lockedUntil (timestamp, nullable) 필드 존재
```

From packages/db/src/schema/refresh-tokens.ts:
```typescript
// refreshTokens 테이블에 userId, tokenHash, expiresAt 필드
// idx_refresh_user 인덱스 존재 (userId 기반 일괄 삭제 효율적)
```

<!-- 테스트 팩토리 -->
From apps/api/tests/helpers/factory.ts:
```typescript
export async function createUser(db: Db, options?: CreateUserOptions): Promise<{ user, accessToken }>;
// 기본 비밀번호: 'Test123!@#'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: changePassword 서비스 메서드 + 통합 테스트</name>
  <files>apps/api/src/services/auth-service.ts, apps/api/tests/integration/password-change.test.ts</files>
  <read_first>
    - apps/api/src/services/auth-service.ts (전체 — 특히 login() 119-195행의 실패 카운트/잠금 패턴, hashToken() 44-46행)
    - apps/api/src/utils/password.ts (comparePassword, hashPassword, validatePassword)
    - apps/api/src/utils/jwt.ts (signTokenPair, getRefreshTokenExpiry)
    - apps/api/tests/helpers/setup.ts (테스트 설정)
    - apps/api/tests/helpers/factory.ts (createUser — 기본 비밀번호 'Test123!@#')
    - packages/db/src/schema/users.ts (loginFailCount, lockedUntil 필드)
    - packages/db/src/schema/refresh-tokens.ts (refreshTokens 테이블 구조)
  </read_first>
  <behavior>
    - Test 1: 올바른 현재 비밀번호 + 유효한 새 비밀번호 → 200, accessToken + refreshToken 반환
    - Test 2: 변경 성공 후 이전 refresh token으로 /auth/refresh 호출 → 실패 (무효화됨)
    - Test 3: 변경 성공 후 새 refresh token으로 /auth/refresh 호출 → 성공
    - Test 4: 잘못된 현재 비밀번호 → 401 INVALID_CREDENTIALS
    - Test 5: 유효하지 않은 새 비밀번호 (8자 미만) → 400 INVALID_PASSWORD
    - Test 6: 현재 비밀번호 5회 연속 실패 → 401 ACCOUNT_LOCKED
    - Test 7: 잠금 해제 후 변경 성공 가능
    - Test 8: 미인증 요청 → 401
  </behavior>
  <action>
    **auth-service.ts에 changePassword 메서드 추가:**

    ```typescript
    async function changePassword(
      userId: number,
      currentPassword: string,
      newPassword: string,
    ): Promise<{ accessToken: string; refreshToken: string }> {
      // 1. DB에서 user 조회 (passwordHash, loginFailCount, lockedUntil, email 필요)
      const found = await db
        .select({
          id: users.id,
          email: users.email,
          passwordHash: users.passwordHash,
          loginFailCount: users.loginFailCount,
          lockedUntil: users.lockedUntil,
        })
        .from(users)
        .where(eq(users.id, userId))
        .limit(1);

      const user = found[0];
      if (!user) throw unauthorized('User not found');

      // 2. 계정 잠금 확인 (D-02: 로그인 정책 동일 적용)
      if (user.lockedUntil && user.lockedUntil > new Date()) {
        const remainingMs = user.lockedUntil.getTime() - Date.now();
        const remainingMin = Math.ceil(remainingMs / 60000);
        throw new AppError('ACCOUNT_LOCKED', `계정이 잠겼습니다. ${remainingMin}분 후에 다시 시도해주세요.`, 401);
      }

      // 3. 현재 비밀번호 검증
      const passwordMatch = await comparePassword(currentPassword, user.passwordHash);
      if (!passwordMatch) {
        // D-02: 실패 카운트 증가 (loginFailCount 재사용)
        const newFailCount = user.loginFailCount + 1;
        const updates: { loginFailCount: number; lockedUntil?: Date; updatedAt: Date } = {
          loginFailCount: newFailCount,
          updatedAt: new Date(),
        };
        if (newFailCount >= 5) {
          updates.lockedUntil = new Date(Date.now() + 15 * 60 * 1000); // 15분 잠금
          logger.warn(`Account locked for user ${userId} due to ${newFailCount} failed password change attempts`);
        }
        await db.update(users).set(updates).where(eq(users.id, userId));
        throw new AppError('INVALID_CREDENTIALS', '현재 비밀번호가 올바르지 않습니다.', 401);
      }

      // 4. 새 비밀번호 유효성 검사
      const validation = validatePassword(newPassword);
      if (!validation.valid) {
        throw badRequest('INVALID_PASSWORD', validation.message ?? 'Invalid password');
      }

      // 5. 새 비밀번호 해시
      const newHash = await hashPassword(newPassword);

      // 6. D-01 + D-03: Atomic 트랜잭션 — 비밀번호 변경 + 전체 세션 무효화 + 새 토큰 발급
      const tokenPair = signTokenPair({ userId: String(userId), email: user.email });
      const newTokenHash = hashToken(tokenPair.refreshToken);
      const expiresAt = getRefreshTokenExpiry(false);

      await db.transaction(async (tx) => {
        // a. 비밀번호 업데이트 + 실패 카운트 리셋
        await tx.update(users).set({
          passwordHash: newHash,
          loginFailCount: 0,
          lockedUntil: null,
          updatedAt: new Date(),
        }).where(eq(users.id, userId));

        // b. 해당 사용자의 모든 refresh token 삭제 (전체 세션 무효화)
        await tx.delete(refreshTokens).where(eq(refreshTokens.userId, userId));

        // c. 새 refresh token 삽입 (현재 세션 유지)
        await tx.insert(refreshTokens).values({
          userId,
          tokenHash: newTokenHash,
          expiresAt,
        });
      });

      logger.info('Password changed successfully', { userId });

      return {
        accessToken: tokenPair.accessToken,
        refreshToken: tokenPair.refreshToken,
      };
    }
    ```

    return 객체에 `changePassword` 추가 (기존 register, verifyEmail, login, refresh, logout 옆에).

    **password-change.test.ts 생성:**
    - setup.ts, factory.ts import
    - createUser()로 테스트 유저 생성 (기본 비밀번호 'Test123!@#')
    - PUT /api/v1/users/me/password 엔드포인트 호출
    - 8개 테스트 케이스 (behavior 참조)
    - refresh token 무효화 검증: 변경 전 로그인으로 refresh token 획득 → 변경 후 해당 token으로 /auth/refresh 호출 → 실패 확인
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/markflow && pnpm --filter @markflow/api test -- --run tests/integration/password-change.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - auth-service.ts에 `async function changePassword(` 함수가 존재
    - auth-service.ts changePassword에 `db.transaction(` 호출이 존재
    - auth-service.ts changePassword 트랜잭션 안에 `tx.delete(refreshTokens)` 가 존재
    - auth-service.ts changePassword 트랜잭션 안에 `tx.insert(refreshTokens)` 가 존재
    - auth-service.ts changePassword에 `comparePassword(` 호출이 존재
    - auth-service.ts changePassword에 `validatePassword(` 호출이 존재
    - auth-service.ts changePassword에 `ACCOUNT_LOCKED` 에러 코드가 존재
    - auth-service.ts return 객체에 `changePassword` 포함
    - password-change.test.ts 파일이 존재하고 최소 8개 테스트 포함
    - 모든 테스트 통과
  </acceptance_criteria>
  <done>changePassword 서비스가 atomic 트랜잭션으로 비밀번호 변경 + 세션 무효화 + 토큰 재발급을 수행하고, 8개 통합 테스트가 모두 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: PUT /me/password 라우트 등록</name>
  <files>apps/api/src/routes/users.ts</files>
  <read_first>
    - apps/api/src/routes/users.ts (현재 라우트 구조 — GET /me, PATCH /me)
    - apps/api/src/routes/auth.ts (authRateLimit 패턴 — line 13-22, getRefreshTokenCookieOptions — line 25-31)
    - apps/api/src/services/auth-service.ts (changePassword 메서드 — Task 1에서 추가됨)
    - apps/api/src/index.ts (라우트 등록 시 db 전달 패턴)
  </read_first>
  <action>
    **users.ts에 PUT /me/password 엔드포인트 추가:**

    1. UsersRoutesOptions에 db를 사용하여 authService 생성:
    ```typescript
    import { createAuthService } from '../services/auth-service.js';
    ```

    2. authRateLimit 헬퍼 함수 추가 (auth.ts 패턴 동일):
    ```typescript
    function authRateLimit(max = 5) {
      return {
        config: {
          rateLimit: {
            max,
            timeWindow: '15 minutes',
            keyGenerator: (request: FastifyRequest) => request.ip,
          },
        },
      };
    }
    ```

    3. PUT /me/password 라우트 추가:
    ```typescript
    const authService = createAuthService(db);
    const IS_PRODUCTION = process.env.NODE_ENV === 'production';

    // PUT /api/v1/users/me/password
    app.put<{ Body: { currentPassword: string; newPassword: string } }>(
      '/me/password',
      authRateLimit(5), // D-02: 5회/15분 rate limit
      async (request: FastifyRequest<{ Body: { currentPassword: string; newPassword: string } }>, reply: FastifyReply) => {
        if (!request.currentUser) {
          throw unauthorized('Not authenticated');
        }

        const { currentPassword, newPassword } = request.body;

        if (!currentPassword || !newPassword) {
          throw badRequest('MISSING_FIELDS', 'currentPassword and newPassword are required');
        }

        const result = await authService.changePassword(
          Number(request.currentUser.userId),
          currentPassword,
          newPassword,
        );

        // D-03: 새 refresh token을 HttpOnly 쿠키에 설정
        reply.setCookie('refreshToken', result.refreshToken, {
          httpOnly: true,
          sameSite: 'strict',
          path: '/api/v1/auth',
          secure: IS_PRODUCTION,
          maxAge: 7 * 24 * 60 * 60, // 7 days in seconds
        });

        return reply.status(200).send({
          accessToken: result.accessToken,
          refreshToken: result.refreshToken,
        });
      },
    );
    ```

    **주의사항:**
    - authMiddleware는 이미 `app.addHook('preHandler', authMiddleware)` 로 전체 적용됨
    - rate limit은 per-route config로 적용 (auth.ts 패턴 동일)
    - cookie path는 `/api/v1/auth` (refresh endpoint 경로와 일치)
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/markflow && pnpm --filter @markflow/api test -- --run tests/integration/password-change.test.ts tests/integration/users-me.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - users.ts에 `'/me/password'` 라우트가 존재
    - users.ts에 `app.put` 또는 `app.put<` 패턴이 존재
    - users.ts에 `authService.changePassword(` 호출이 존재
    - users.ts에 `currentPassword` 와 `newPassword` body 파라미터 검증이 존재
    - users.ts에 `setCookie('refreshToken'` 호출이 존재
    - users.ts에 `createAuthService` import가 존재
    - password-change.test.ts 전체 통과
    - users-me.test.ts 전체 통과 (기존 GET /me, PATCH /me 회귀 없음)
  </acceptance_criteria>
  <done>PUT /api/v1/users/me/password 엔드포인트가 등록되어 rate limit, 인증, 쿠키 설정이 적용되고, 통합 테스트가 모두 통과한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> PUT /me/password | 인증된 사용자의 비밀번호 변경 요청. currentPassword 검증이 핵심. |
| changePassword -> DB transaction | 세션 무효화와 새 토큰 발급의 atomic 처리 경계. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Spoofing | PUT /me/password | mitigate | currentPassword를 bcrypt comparePassword()로 검증, 실패 시 loginFailCount 증가 |
| T-01-12 | Spoofing | Brute-force attack | mitigate | D-02: 5회 실패 시 15분 잠금 (loginFailCount/lockedUntil 재사용) + IP rate limit 5req/15min |
| T-01-13 | Elevation | Session fixation | mitigate | D-01: 비밀번호 변경 시 전체 refresh token 삭제 (db.transaction 내 atomic) |
| T-01-14 | Repudiation | Password change | mitigate | logger.info로 비밀번호 변경 이벤트 기록 (userId) |
| T-01-15 | Tampering | Race condition | mitigate | db.transaction()으로 DELETE refreshTokens + INSERT new token을 atomic 처리 |
| T-01-16 | Information Disclosure | Error messages | mitigate | 현재 비밀번호 틀림 시 '현재 비밀번호가 올바르지 않습니다.' (이메일 노출 없음) |
</threat_model>

<verification>
1. PUT /me/password + 올바른 비밀번호 → 200, accessToken + refreshToken 반환
2. PUT /me/password + 틀린 비밀번호 → 401 INVALID_CREDENTIALS
3. 5회 실패 → 401 ACCOUNT_LOCKED
4. 변경 후 이전 refresh token → /auth/refresh 실패
5. 변경 후 새 refresh token → /auth/refresh 성공
6. 미인증 → 401
7. 기존 users-me.test.ts 통과 (회귀 없음)
</verification>

<success_criteria>
- PUT /api/v1/users/me/password가 동작하고 올바른 비밀번호로 변경 성공
- 변경 성공 시 새 access + refresh token이 응답에 포함됨
- 변경 성공 시 이전 세션의 refresh token이 모두 무효화됨
- 5회 실패 시 계정 잠금이 적용됨
- 통합 테스트 8개 이상 통과
- 기존 users-me 테스트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-hardening/01-03-SUMMARY.md`
</output>
