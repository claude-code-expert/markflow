---
phase: 01-security-auth-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/graph-service.ts
  - apps/api/src/services/relation-service.ts
  - apps/api/tests/integration/graph.test.ts
  - apps/api/tests/integration/relations.test.ts
  - docs/SECURITY.md
autonomous: true
requirements:
  - SEC-03
  - SEC-04
  - SEC-05
must_haves:
  truths:
    - "graph-service가 documentRelations 전체 테이블이 아닌 workspace 소속 문서의 relation만 조회한다"
    - "relation-service getRelations()가 N+1 없이 단일 JOIN 쿼리로 동작한다"
    - "relation-service detectCycle()이 루프 내 DB 쿼리 없이 사이클을 감지한다"
    - "SECURITY.md에 Avatar SVG 거부 / Editor SVG 허용 분리 근거가 문서화되어 있다"
  artifacts:
    - path: "apps/api/src/services/graph-service.ts"
      provides: "Workspace-scoped relation query"
      contains: "inArray"
    - path: "apps/api/src/services/relation-service.ts"
      provides: "JOIN-based batch query for getRelations, batch preload for detectCycle"
      contains: "innerJoin"
    - path: "docs/SECURITY.md"
      provides: "SVG security separation documentation"
      contains: "Avatar"
  key_links:
    - from: "apps/api/src/services/graph-service.ts"
      to: "documentRelations table"
      via: "inArray(sourceId, docIdArray) AND inArray(targetId, docIdArray)"
      pattern: "inArray.*documentRelations"
    - from: "apps/api/src/services/relation-service.ts"
      to: "documents table"
      via: "innerJoin for batch document lookup"
      pattern: "innerJoin.*documents"
---

<objective>
graph-service의 전체 테이블 스캔을 workspace 범위 쿼리로 교체하고, relation-service의 N+1 쿼리를 JOIN 배치로 리팩터링하며, SVG 보안 분리 규칙을 문서화한다.

Purpose: 데이터 격리(워크스페이스 범위 강제)와 쿼리 성능(N+1 제거)을 확보하고, SVG 보안 정책을 명문화하여 미래 개발자 참조 가능하게 한다.
Output: 최적화된 graph-service, relation-service, SVG 보안 문서, 검증 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-hardening/01-CONTEXT.md
@.planning/phases/01-security-auth-hardening/01-RESEARCH.md

<interfaces>
<!-- graph-service 현재 코드 — 전체 스캔 문제 -->
From apps/api/src/services/graph-service.ts:
```typescript
interface GraphNode { id: number; title: string; categoryId: number | null; }
interface GraphEdge { source: number; target: number; type: 'prev' | 'next' | 'related'; }
interface WorkspaceGraph { nodes: GraphNode[]; edges: GraphEdge[]; }

export function createGraphService(db: Db) {
  async function getWorkspaceGraph(workspaceId: string): Promise<WorkspaceGraph>
  // 문제: line 56-62에서 documentRelations 전체 SELECT 후 JS 필터
}
```

<!-- relation-service 현재 코드 — N+1 문제 -->
From apps/api/src/services/relation-service.ts:
```typescript
interface RelationDoc { id: number; title: string; }
interface RelationsResult { prev: RelationDoc | null; next: RelationDoc | null; related: RelationDoc[]; }

export function createRelationService(db: Db) {
  async function detectCycle(docId: string, targetId: string, direction: 'prev' | 'next'): Promise<boolean>
  // 문제: while 루프 안에서 개별 db.select() 2회씩 (line 90-135)

  async function getRelations(docId: string): Promise<RelationsResult>
  // 문제: for 루프 안에서 개별 document SELECT (line 280-301)
}
```

<!-- Drizzle ORM 사용 가능 함수들 -->
From @markflow/db exports:
```typescript
import { documentRelations, documents, eq, and, or, inArray } from '@markflow/db';
// innerJoin은 db.select().from().innerJoin() 체이닝으로 사용
```

<!-- 기존 테스트 패턴 -->
From apps/api/tests/integration/graph.test.ts:
```typescript
import { getApp, getDb } from '../helpers/setup.js';
import { createUser, createWorkspace, addMember } from '../helpers/factory.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: graph-service workspace 범위 쿼리 최적화 (SEC-04)</name>
  <files>apps/api/src/services/graph-service.ts, apps/api/tests/integration/graph.test.ts</files>
  <read_first>
    - apps/api/src/services/graph-service.ts (전체 — 특히 56-62행 전체 테이블 스캔 부분)
    - apps/api/tests/integration/graph.test.ts (기존 테스트 — 확장 대상)
    - packages/db/src/schema/document-relations.ts (인덱스 확인)
  </read_first>
  <behavior>
    - Test 1: 워크스페이스 A의 그래프 조회 시 워크스페이스 B의 relation이 포함되지 않음
    - Test 2: 빈 워크스페이스(문서 0개) 그래프 조회 시 빈 결과 반환 (에러 아님)
    - Test 3: 워크스페이스 내 문서 간 relation이 정상적으로 edges에 포함됨
    - Test 4: 기존 graph.test.ts의 모든 테스트가 통과 (회귀 없음)
  </behavior>
  <action>
    **graph-service.ts 수정 — per D-07 (응답 확장 허용), Claude's Discretion (inArray 선택):**

    line 56-74의 전체 스캔 + JS 필터 코드를 inArray 기반 DB 쿼리로 교체:

    ```typescript
    // 기존 (제거):
    // const allRelations = await db.select({...}).from(documentRelations);
    // for 루프 JS 필터...

    // 수정 후:
    const docIdArray = docs.map((d) => d.id);
    const relations = await db
      .select({
        sourceId: documentRelations.sourceId,
        targetId: documentRelations.targetId,
        type: documentRelations.type,
      })
      .from(documentRelations)
      .where(
        and(
          inArray(documentRelations.sourceId, docIdArray),
          inArray(documentRelations.targetId, docIdArray),
        ),
      );

    const edges: GraphEdge[] = relations.map((rel) => ({
      source: rel.sourceId,
      target: rel.targetId,
      type: rel.type as 'prev' | 'next' | 'related',
    }));
    ```

    - `docs.length === 0` 가드는 기존 line 52-54에 이미 있으므로 유지
    - inArray에 빈 배열이 전달되지 않도록 가드 유지가 중요

    **graph.test.ts 확장:**
    - "cross-workspace isolation" 테스트 추가: 워크스페이스 2개 생성, 각각 문서 생성, 관계 설정. 워크스페이스 A 그래프 조회 시 B의 관계 미포함 확인.
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/markflow && pnpm --filter @markflow/api test -- --run tests/integration/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - graph-service.ts에 `.from(documentRelations)` 다음에 `.where(` 가 있음 (전체 스캔 제거)
    - graph-service.ts에 `inArray(documentRelations.sourceId` 패턴이 있음
    - graph-service.ts에 `inArray(documentRelations.targetId` 패턴이 있음
    - graph-service.ts에 `allRelations` 변수가 없음 (전체 스캔 변수 제거)
    - graph-service.ts에 `docIds.has(` 패턴이 없음 (JS 필터 제거)
    - graph.test.ts에 "cross-workspace" 또는 "isolation" 키워드가 포함된 테스트 존재
    - graph.test.ts 전체 통과
  </acceptance_criteria>
  <done>graph-service가 inArray로 workspace 소속 문서의 relation만 DB에서 조회하고, cross-workspace isolation 테스트가 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: relation-service N+1 제거 + detectCycle 배치 최적화 (SEC-05, D-08)</name>
  <files>apps/api/src/services/relation-service.ts, apps/api/tests/integration/relations.test.ts</files>
  <read_first>
    - apps/api/src/services/relation-service.ts (전체 — 특히 63-139행 detectCycle, 266-308행 getRelations)
    - apps/api/tests/integration/relations.test.ts (기존 테스트)
    - apps/api/tests/integration/circular-ref.test.ts (cycle detection 테스트 — detectCycle 동작 검증)
    - apps/api/src/services/comment-service.ts (innerJoin 패턴 참조)
  </read_first>
  <behavior>
    - Test 1: getRelations()이 단일 쿼리로 prev/next/related를 반환 (기존 기능 동일)
    - Test 2: getRelations()에서 삭제된 문서는 결과에 포함되지 않음
    - Test 3: detectCycle()이 기존과 동일하게 cycle을 감지
    - Test 4: circular-ref.test.ts의 기존 테스트 전체 통과 (회귀 없음)
    - Test 5: relations.test.ts의 기존 테스트 전체 통과 (회귀 없음)
  </behavior>
  <action>
    **getRelations() N+1 수정 — per D-07 (응답 확장 허용):**

    line 266-308의 for 루프 + 개별 SELECT를 단일 innerJoin 쿼리로 교체:

    ```typescript
    async function getRelations(docId: string): Promise<RelationsResult> {
      const rows = await db
        .select({
          type: documentRelations.type,
          targetId: documentRelations.targetId,
          docId: documents.id,
          docTitle: documents.title,
        })
        .from(documentRelations)
        .innerJoin(
          documents,
          and(
            eq(documentRelations.targetId, documents.id),
            eq(documents.isDeleted, false),
          ),
        )
        .where(eq(documentRelations.sourceId, Number(docId)));

      let prevDoc: RelationDoc | null = null;
      let nextDoc: RelationDoc | null = null;
      const relatedDocs: RelationDoc[] = [];

      for (const row of rows) {
        const doc = { id: row.docId, title: row.docTitle };
        if (row.type === 'prev') prevDoc = doc;
        else if (row.type === 'next') nextDoc = doc;
        else if (row.type === 'related') relatedDocs.push(doc);
      }

      return { prev: prevDoc, next: nextDoc, related: relatedDocs };
    }
    ```

    **detectCycle() 배치 최적화 — per D-08:**

    while 루프 내 개별 쿼리를 배치 preload + 메모리 내 탐색으로 교체:

    ```typescript
    async function detectCycle(
      docId: string,
      targetId: string,
      direction: 'prev' | 'next',
    ): Promise<boolean> {
      const numDocId = Number(docId);
      const numTargetId = Number(targetId);
      const startNode = direction === 'prev' ? numDocId : numTargetId;
      const searchFor = direction === 'prev' ? numTargetId : numDocId;

      // 배치 preload: type='next'인 모든 relation을 한 번에 로드
      const nextRelations = await db
        .select({
          sourceId: documentRelations.sourceId,
          targetId: documentRelations.targetId,
        })
        .from(documentRelations)
        .where(eq(documentRelations.type, 'next'));

      // Map<sourceId, targetId> 구성
      const nextMap = new Map<number, number>();
      for (const rel of nextRelations) {
        nextMap.set(rel.sourceId, rel.targetId);
      }

      // Forward traversal: startNode -> ... -> searchFor?
      const visited = new Set<number>();
      let current: number | null = startNode;
      while (current !== null) {
        if (visited.has(current)) break;
        visited.add(current);
        const next = nextMap.get(current) ?? null;
        if (next === searchFor) return true;
        current = next;
      }

      // Reverse traversal: searchFor -> ... -> startNode?
      const visited2 = new Set<number>();
      let current2: number | null = searchFor;
      while (current2 !== null) {
        if (visited2.has(current2)) break;
        visited2.add(current2);
        const next = nextMap.get(current2) ?? null;
        if (next === startNode) return true;
        current2 = next;
      }

      return false;
    }
    ```

    **주의:** detectCycle의 배치 preload는 전체 next relation을 로드함. MVP 규모에서 문제 없지만, 향후 workspace scope를 좁힐 수 있음 (setRelations에서 workspaceId를 detectCycle에 전달).

    **relations.test.ts 확장:**
    - 기존 테스트에 "getRelations returns joined results" 검증이 포함되어 있으므로 기존 테스트가 리팩터링 후에도 통과하는지 확인
    - 추가: 삭제된 문서가 relation 결과에서 제외되는지 명시적 테스트
  </action>
  <verify>
    <automated>cd /Users/codevillain/Claude-Code-Expert/markflow && pnpm --filter @markflow/api test -- --run tests/integration/relations.test.ts tests/integration/circular-ref.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - relation-service.ts getRelations()에 `innerJoin(` 패턴이 있음
    - relation-service.ts getRelations()에 for 루프 내 `await db` 패턴이 없음 (N+1 제거)
    - relation-service.ts detectCycle()에 `nextMap` 또는 유사한 Map 변수가 있음 (배치 preload)
    - relation-service.ts detectCycle()의 while 루프 안에 `await db` 패턴이 없음 (루프 내 쿼리 제거)
    - relations.test.ts 전체 통과
    - circular-ref.test.ts 전체 통과
  </acceptance_criteria>
  <done>getRelations()이 단일 JOIN 쿼리로 동작하고, detectCycle()이 배치 preload + 메모리 탐색으로 동작하며, 기존 기능 테스트 전체 통과</done>
</task>

<task type="auto">
  <name>Task 3: SECURITY.md SVG 분리 규칙 문서화 (SEC-03, D-09)</name>
  <files>docs/SECURITY.md</files>
  <read_first>
    - docs/SECURITY.md (현재 문서 전체 — 이미지 업로드 보안 섹션 26-29행)
    - .planning/phases/01-security-auth-hardening/01-CONTEXT.md (D-09 결정)
    - apps/worker/src/index.ts (SVG 허용 타입 확인)
  </read_first>
  <action>
    **Per D-09: SECURITY.md에 규칙 + 근거 형태로 SVG 보안 분리 문서화**

    `docs/SECURITY.md`의 "이미지 업로드 보안" 섹션 아래에 새로운 하위 섹션 추가:

    ```markdown
    ### SVG 파일 보안 — Avatar/Editor 분리 정책

    #### 규칙

    | 컨텍스트 | SVG 허용 | 렌더링 방식 | 위험도 |
    |----------|----------|------------|--------|
    | **Avatar (프로필 이미지)** | 거부 | `<img src="...">` 태그 | 높음 — `<img>` 태그로 SVG를 렌더링하면 내장 스크립트가 실행될 수 있음 |
    | **Editor (문서 본문 이미지)** | 허용 | `<img src="...">` + rehype-sanitize | 낮음 — 에디터 출력은 반드시 rehype-sanitize를 통과한 HTML에서만 렌더링 |

    #### 근거

    **Avatar에서 SVG를 거부하는 이유:**
    - Avatar는 `<img>` 태그로 렌더링되며, 브라우저에 따라 SVG 내 `<script>` 태그, `onload` 이벤트 핸들러, `<foreignObject>` 등이 실행될 수 있음
    - Avatar URL은 프로필 카드, 댓글, 멤버 목록 등 다양한 곳에서 렌더링되므로 XSS 공격 표면이 넓음
    - Avatar 업로드 시 서버 측에서 SVG MIME 타입(`image/svg+xml`)을 거부하여 원천 차단

    **Editor에서 SVG를 허용하는 이유:**
    - 문서 본문의 이미지는 마크다운 `![alt](url)` 문법으로 삽입되며, `<img>` 태그로 렌더링됨
    - 에디터의 HTML 출력은 반드시 `parseMarkdown()` -> `rehype-sanitize`를 거침
    - rehype-sanitize가 `<script>`, `on*` 이벤트 속성, `javascript:` URL 스킴을 모두 제거
    - SVG 파일 자체는 R2에 저장되고, `<img src="r2-url">` 태그로만 참조되므로 내부 스크립트가 실행되지 않음
    - 기술 문서에서 SVG 다이어그램(flowchart, 아키텍처도 등)은 필수 요소

    #### 구현 체크리스트

    - [ ] Avatar 업로드 API: `image/svg+xml` MIME 타입 거부 (400 에러)
    - [x] R2 Worker: `image/svg+xml` 허용 (문서 이미지용)
    - [x] Editor Preview: `rehype-sanitize` 적용 (`parseMarkdown()`)
    - [x] `dangerouslySetInnerHTML`: sanitize 통과한 HTML에만 사용
    - [ ] SVG를 `<img>` 태그 외의 방식(inline SVG, `<object>`, `<embed>`)으로 렌더링하지 않음
    ```

    이 내용을 기존 "이미지 업로드 보안" 섹션의 기존 SVG 관련 한 줄 (`- SVG 업로드: ...`) 아래에 삽입한다. 기존 한 줄은 유지하되 새 섹션으로 상세 내용을 보강하는 형태.
  </action>
  <verify>
    <automated>grep -c "Avatar.*SVG.*거부" /Users/codevillain/Claude-Code-Expert/markflow/docs/SECURITY.md && grep -c "Editor.*SVG.*허용" /Users/codevillain/Claude-Code-Expert/markflow/docs/SECURITY.md && grep -c "rehype-sanitize" /Users/codevillain/Claude-Code-Expert/markflow/docs/SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md에 "SVG 파일 보안" 또는 "Avatar/Editor 분리" 키워드가 포함된 섹션 존재
    - SECURITY.md에 "Avatar" + "거부" 키워드가 같은 문단에 존재
    - SECURITY.md에 "Editor" + "허용" 키워드가 같은 문단에 존재
    - SECURITY.md에 "근거" 섹션이 존재 (왜 분리했는지 설명)
    - SECURITY.md에 "rehype-sanitize" 참조가 SVG 섹션에 존재
    - 기존 XSS 방어/이미지 업로드 보안 섹션이 손상되지 않음
  </acceptance_criteria>
  <done>SECURITY.md에 Avatar(SVG 거부)/Editor(SVG 허용) 분리 정책이 규칙 + 근거 형태로 문서화되어 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Database | graph-service/relation-service가 DB 쿼리로 데이터 조회. workspace 범위 강제가 핵심 |
| User -> Document content | SVG 파일이 포함된 마크다운 콘텐츠. sanitize 파이프라인 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | graph-service | mitigate | inArray로 workspace 범위 제한, 전체 테이블 스캔 제거 |
| T-01-07 | Denial of Service | relation-service | mitigate | N+1 쿼리를 단일 JOIN으로 교체, detectCycle 배치 preload |
| T-01-08 | Information Disclosure | graph-service | mitigate | inArray(sourceId, docIdArray) AND inArray(targetId, docIdArray)로 cross-workspace relation 노출 방지 |
| T-01-09 | Tampering | SVG in avatar | mitigate | Avatar에서 SVG MIME 거부 정책 문서화 (구현은 이미 존재하거나 Phase 2에서 완성) |
| T-01-10 | Tampering | SVG in editor | accept | rehype-sanitize가 모든 위험 요소 제거, img 태그로만 렌더링 |
</threat_model>

<verification>
1. graph-service: 워크스페이스 A 문서만의 relation 조회 확인 (cross-workspace isolation)
2. relation-service: getRelations()가 for 루프 내 await 없이 동작 확인
3. relation-service: detectCycle()이 while 루프 내 await 없이 동작 확인
4. circular-ref.test.ts 전체 통과 (cycle detection 회귀 없음)
5. relations.test.ts 전체 통과 (relation 기능 회귀 없음)
6. graph.test.ts 전체 통과 (graph 기능 회귀 없음)
7. SECURITY.md에 SVG 분리 규칙 + 근거 존재 확인
</verification>

<success_criteria>
- graph-service에서 전체 테이블 스캔 코드가 완전히 제거됨
- relation-service getRelations()에서 N+1 패턴이 제거됨
- relation-service detectCycle()에서 루프 내 쿼리가 제거됨
- 기존 통합 테스트(graph, relations, circular-ref) 전체 통과
- SECURITY.md에 SVG Avatar/Editor 분리 규칙 + 근거 문서화 완료
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-hardening/01-02-SUMMARY.md`
</output>
