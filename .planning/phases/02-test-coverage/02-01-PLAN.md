---
phase: 02-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/tests/helpers/factory.ts
  - apps/api/tests/integration/comments.test.ts
autonomous: true
requirements:
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "댓글 생성 시 201 응답과 content/authorId/authorName이 반환된다"
    - "댓글 목록 조회 시 200 응답과 createdAt 오름차순 배열이 반환된다"
    - "댓글 수정 시 200 응답과 변경된 content가 반환된다"
    - "댓글 삭제 시 204 응답이 반환되고 DB에서 삭제된다"
    - "댓글 해결(resolved) 토글 시 resolved=true, resolvedBy가 설정된다"
    - "viewer 역할은 댓글 생성 시 403이 반환된다"
    - "타인의 댓글 수정/삭제 시 403이 반환된다"
    - "parentId로 대댓글 생성 시 201 응답과 parentId가 반환된다"
  artifacts:
    - path: "apps/api/tests/helpers/factory.ts"
      provides: "createDocument(), createComment() 팩토리 함수"
      exports: ["createDocument", "createComment"]
    - path: "apps/api/tests/integration/comments.test.ts"
      provides: "댓글 CRUD 7개 시나리오 통합 테스트"
      contains: "describe.*comments"
  key_links:
    - from: "apps/api/tests/integration/comments.test.ts"
      to: "apps/api/tests/helpers/factory.ts"
      via: "import { createDocument, createComment }"
      pattern: "import.*createDocument.*createComment.*factory"
    - from: "apps/api/tests/integration/comments.test.ts"
      to: "apps/api/src/routes/comments.ts"
      via: "app.inject() HTTP 호출"
      pattern: "app\\.inject.*comments"
---

<objective>
댓글 CRUD 통합 테스트 7개 시나리오를 작성하여 TEST-01 요구사항을 충족한다.

Purpose: Phase 1 보안 변경과 기존 댓글 기능이 정상 동작함을 통합 테스트로 검증하여 회귀를 방지한다.
Output: factory.ts에 createDocument()/createComment() 헬퍼 추가, comments.test.ts에 7개 시나리오 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-coverage/02-CONTEXT.md
@.planning/phases/02-test-coverage/02-RESEARCH.md

<interfaces>
<!-- 기존 factory.ts 패턴 — executor가 동일 패턴으로 확장해야 함 -->

From apps/api/tests/helpers/factory.ts:
```typescript
export async function createUser(db: Db, options: CreateUserOptions = {}): Promise<{ user: User; accessToken: string }>;
export async function createWorkspace(db: Db, ownerId: number, options: CreateWorkspaceOptions = {}): Promise<Workspace>;
export async function addMember(db: Db, workspaceId: number, userId: number, role: 'admin' | 'editor' | 'viewer'): Promise<Member>;
```

From apps/api/tests/helpers/setup.ts:
```typescript
export function getDb(): Db;
export function getApp(): FastifyInstance;
// beforeAll: buildApp(), DB 연결
// beforeEach: TRUNCATE CASCADE 모든 테이블, resetCounter()
// afterAll: app.close()
```

From apps/api/tests/integration/graph.test.ts (createDocument 로컬 헬퍼 — factory로 이동 대상):
```typescript
async function createDocument(
  app: ReturnType<typeof getApp>,
  wsId: number,
  accessToken: string,
  title: string,
) {
  const res = await app.inject({
    method: 'POST',
    url: `/api/v1/workspaces/${wsId}/documents`,
    headers: { authorization: `Bearer ${accessToken}` },
    payload: { title },
  });
  expect(res.statusCode).toBe(201);
  const doc = (res.json() as { document: { id: number; title: string } }).document;
  return { id: String(doc.id), title: doc.title };
}
```

From apps/api/src/routes/comments.ts (4개 엔드포인트):
```
GET  /workspaces/:wsId/documents/:docId/comments    — requireRole('viewer')
POST /workspaces/:wsId/documents/:docId/comments    — requireRole('editor')
PATCH /workspaces/:wsId/documents/:docId/comments/:commentId — requireRole('editor')
DELETE /workspaces/:wsId/documents/:docId/comments/:commentId — requireRole('editor')
```

From apps/api/src/services/comment-service.ts (핵심 비즈니스 규칙):
- create(): content 필수, 5000자 제한, parentId 지정 시 같은 document에 존재해야 함
- update(): 작성자만 수정 가능 (comment.authorId !== Number(userId) → forbidden)
- toggleResolved(): resolved 토글, resolvedBy = userId (해제 시 null)
- remove(): 작성자만 삭제 가능 (comment.authorId !== Number(userId) → forbidden)

From packages/db/src/schema/comments.ts:
```typescript
export const comments = pgTable('comments', {
  id: bigserial('id', { mode: 'number' }).primaryKey(),
  documentId: bigint('document_id', { mode: 'number' }).notNull().references(() => documents.id, { onDelete: 'cascade' }),
  authorId: bigint('author_id', { mode: 'number' }).notNull().references(() => users.id),
  content: text('content').notNull(),
  parentId: bigint('parent_id', { mode: 'number' }).references((): AnyPgColumn => comments.id),
  resolved: boolean('resolved').notNull().default(false),
  resolvedBy: bigint('resolved_by', { mode: 'number' }).references(() => users.id),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: factory.ts에 createDocument(), createComment() 헬퍼 추가 (per D-05)</name>
  <files>apps/api/tests/helpers/factory.ts</files>
  <read_first>
    - apps/api/tests/helpers/factory.ts
    - apps/api/tests/integration/graph.test.ts (lines 11-27, createDocument 로컬 헬퍼 패턴)
    - apps/api/src/routes/comments.ts
    - packages/db/src/schema/comments.ts
    - packages/db/src/schema/documents.ts
  </read_first>
  <behavior>
    - Test 1: createDocument(app, wsId, accessToken, 'Test Doc')가 { id: number, title: string } 객체를 반환한다
    - Test 2: createComment(db, documentId, authorId)가 { id: number, content: string, authorId: number, documentId: number } 객체를 반환한다
    - Test 3: createComment(db, documentId, authorId, { parentId })가 parentId가 설정된 댓글을 반환한다
    - Test 4: createComment(db, documentId, authorId, { content: 'custom' })가 지정한 content로 생성한다
  </behavior>
  <action>
    factory.ts에 다음 두 함수를 추가한다. 기존 import 목록에 `comments`, `documents`를 `@markflow/db`에서 추가하고, `FastifyInstance` 타입을 `fastify`에서 import한다.

    **createDocument 함수:**
    - 시그니처: `export async function createDocument(app: FastifyInstance, wsId: number, accessToken: string, title?: string)`
    - `app.inject({ method: 'POST', url: '/api/v1/workspaces/${wsId}/documents', headers: { authorization: 'Bearer ${accessToken}' }, payload: { title: title ?? 'Test Document ${nextId()}' } })` 호출
    - 응답을 `(res.json() as { document: { id: number; title: string } }).document`로 파싱
    - `{ id: doc.id, title: doc.title }` 반환 (id는 number 타입으로 반환 — graph.test.ts는 String(doc.id) 했지만 factory에서는 원본 number 유지)

    **createComment 함수:**
    - 시그니처: `export async function createComment(db: Db, documentId: number, authorId: number, options?: { content?: string; parentId?: number })`
    - `db.insert(comments).values({ documentId, authorId, content: options?.content ?? 'Test comment ${nextId()}', parentId: options?.parentId ?? null }).returning()` 호출
    - `comment!` 반환

    기존 factory.ts 상단 import에 추가:
    ```typescript
    import { users, workspaces, workspaceMembers, invitations, joinRequests, comments } from '@markflow/db';
    import type { FastifyInstance } from 'fastify';
    ```
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - factory.ts 파일에 `export async function createDocument(` 문자열이 존재
    - factory.ts 파일에 `export async function createComment(` 문자열이 존재
    - factory.ts 파일에 `import type { FastifyInstance }` 문자열이 존재
    - factory.ts 파일에 `comments` import가 `@markflow/db`에서 존재
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>createDocument()와 createComment()가 factory.ts에 추가되어 기존 패턴과 동일한 인터페이스로 사용 가능하다. TypeScript 컴파일 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: comments.test.ts에 7개 시나리오 통합 테스트 작성 (per D-03, D-04, D-06)</name>
  <files>apps/api/tests/integration/comments.test.ts</files>
  <read_first>
    - apps/api/tests/helpers/factory.ts (Task 1 결과 포함)
    - apps/api/tests/helpers/setup.ts
    - apps/api/tests/integration/auth-login.test.ts (기존 테스트 패턴 참고)
    - apps/api/src/routes/comments.ts
    - apps/api/src/services/comment-service.ts
    - apps/api/src/middleware/rbac.ts
  </read_first>
  <behavior>
    — 생성 시나리오 —
    - Test 1: POST /workspaces/:wsId/documents/:docId/comments → 201, body.comment.content === 'Test comment', body.comment.authorId === user.id, body.comment.authorName 존재
    - Test 2: POST with empty content → 400 (MISSING_FIELDS)
    - Test 3: POST with content > 5000 chars → 400 (INVALID_FIELDS)

    — 조회 시나리오 —
    - Test 4: GET /workspaces/:wsId/documents/:docId/comments → 200, body.comments 배열, authorName 포함, createdAt 오름차순

    — 수정 시나리오 —
    - Test 5: PATCH /.../:commentId with { content: 'Updated' } → 200, body.comment.content === 'Updated'

    — 삭제 시나리오 —
    - Test 6: DELETE /.../:commentId → 204, 이후 GET으로 확인 시 삭제됨

    — 해결 시나리오 —
    - Test 7: PATCH /.../:commentId with { resolved: true } → 200, body.comment.resolved === true, body.comment.resolvedBy === user.id

    — 권한 시나리오 —
    - Test 8: viewer가 POST → 403
    - Test 9: 다른 editor가 PATCH (content 수정) → 403 ('You can only edit your own comments')
    - Test 10: 다른 editor가 DELETE → 403 ('You can only delete your own comments')
    - Test 11: 인증 없이 POST → 401

    — 스레딩 시나리오 —
    - Test 12: POST with parentId → 201, body.comment.parentId === parent.id
    - Test 13: POST with invalid parentId → 404 ('Parent comment not found')
  </behavior>
  <action>
    `apps/api/tests/integration/comments.test.ts` 파일을 새로 생성한다. 기존 `auth-login.test.ts` 패턴(T027)을 따르되 테스트 ID는 T127로 부여한다.

    파일 구조 (per D-03 — 단일 파일, describe 블록으로 7개 시나리오 그룹화):
    ```
    /**
     * T127 -- Comment CRUD Integration Tests
     *
     * 댓글 생성/조회/수정/삭제/해결/권한/스레딩 7개 시나리오
     */
    import { describe, it, expect } from 'vitest';
    import { getApp, getDb } from '../helpers/setup.js';
    import { createUser, createWorkspace, addMember, createDocument, createComment } from '../helpers/factory.js';

    const BASE_URL = '/api/v1/workspaces';

    // 헬퍼 함수: 댓글 URL 생성
    function commentUrl(wsId: number, docId: number, commentId?: number) { ... }

    describe('Comments API (/api/v1/workspaces/:wsId/documents/:docId/comments)', () => {
      describe('Create (POST)', () => { ... });        // 시나리오 1: 생성
      describe('List (GET)', () => { ... });            // 시나리오 2: 조회
      describe('Update (PATCH)', () => { ... });        // 시나리오 3: 수정
      describe('Delete (DELETE)', () => { ... });       // 시나리오 4: 삭제
      describe('Resolve (PATCH resolved)', () => { ... }); // 시나리오 5: 해결
      describe('Permissions', () => { ... });           // 시나리오 6: 권한 (per D-04)
      describe('Threading', () => { ... });             // 시나리오 7: 스레딩
    });
    ```

    각 `it` 블록에서:
    1. `getApp()`, `getDb()` 호출
    2. `createUser(db)` → owner + accessToken
    3. `createWorkspace(db, owner.id)` → workspace
    4. `createDocument(app, ws.id, accessToken, 'Test Doc')` → document (id는 number)
    5. 필요시 `createComment(db, doc.id, owner.id)` 또는 `app.inject()` 로 댓글 생성
    6. `app.inject()` 로 해당 엔드포인트 호출
    7. `expect()` 로 상태코드 및 응답 body 검증

    **권한 시나리오 세부 (per D-04):**
    - viewer: `createUser(db)` → viewer 생성, `addMember(db, ws.id, viewer.id, 'viewer')` → POST 댓글 시 403
    - 타인 수정/삭제: owner가 댓글 작성 → 별도 editor 사용자가 PATCH/DELETE 시 403
    - 인증 없이: headers 없이 POST 시 401

    **해결 시나리오 세부:**
    - PATCH with `{ resolved: true }` — 응답 body의 `comment.resolved === true`, `comment.resolvedBy` 가 요청자 userId와 일치

    **스레딩 시나리오 세부:**
    - parentId 지정: parent 댓글 생성 후 POST with `{ content: 'Reply', parentId: parent.id }` → 201, `comment.parentId === parent.id`
    - invalid parentId: `{ content: 'Reply', parentId: 999999 }` → 404

    URL 패턴: `/api/v1/workspaces/${ws.id}/documents/${doc.id}/comments` (wsId, docId는 number)

    **주의사항:**
    - DB 격리는 setup.ts의 기존 패턴 사용 (per D-06) — 별도 설정 불필요
    - `as` type assertion 시 구체적 인터페이스 사용, `any` 금지
    - 응답 body 타입은 `res.json() as { comment: { id: number; content: string; ... } }` 형태로
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/api test -- tests/integration/comments.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/tests/integration/comments.test.ts 파일이 존재
    - 파일에 `T127` 테스트 ID가 주석으로 존재
    - 파일에 `describe('Create (POST)'` 문자열이 존재
    - 파일에 `describe('List (GET)'` 문자열이 존재
    - 파일에 `describe('Update (PATCH)'` 문자열이 존재
    - 파일에 `describe('Delete (DELETE)'` 문자열이 존재
    - 파일에 `describe('Resolve` 문자열이 존재
    - 파일에 `describe('Permissions'` 문자열이 존재
    - 파일에 `describe('Threading'` 문자열이 존재
    - 파일에 `addMember(db, ws.id,` 패턴이 존재 (viewer 권한 테스트)
    - 파일에 `expect(res.statusCode).toBe(403)` 패턴이 존재
    - 파일에 `expect(res.statusCode).toBe(201)` 패턴이 존재
    - 파일에 `expect(res.statusCode).toBe(204)` 패턴이 존재
    - `pnpm --filter @markflow/api test -- tests/integration/comments.test.ts` 가 exit code 0으로 완료
    - 테스트 출력에 "7 passed" 이상 (13개 it 블록 기대)
  </acceptance_criteria>
  <done>comments.test.ts가 7개 시나리오(생성/조회/수정/삭제/해결/권한/스레딩) 13개+ 테스트를 포함하며 모두 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → API | 테스트 코드가 app.inject()를 통해 HTTP 엔드포인트 호출 — 실제 미들웨어 체인 통과 |
| factory → DB | 팩토리 함수가 DB 직접 삽입 — 미들웨어 우회 가능 (createComment은 DB 직접, createDocument은 API 호출) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | comments.test.ts | mitigate | 인증 없이 POST 시 401 반환 테스트를 Permissions describe 블록에 포함 |
| T-02-02 | Elevation of Privilege | comments.test.ts | mitigate | viewer 역할이 댓글 생성 시 403 반환 테스트, 타인 댓글 수정/삭제 시 403 반환 테스트 포함 |
| T-02-03 | Information Disclosure | factory.ts | accept | factory.ts는 테스트 전용 코드로 devDependencies에만 존재. 하드코딩된 비밀번호('Test123!@#')는 테스트 DB에서만 사용되며 _test suffix 검증으로 운영 DB 접근 차단 |
| T-02-04 | Tampering | comments.test.ts | mitigate | authorId 검증 테스트로 타인 댓글 수정/삭제 차단 동작을 검증 |
</threat_model>

<verification>
1. `pnpm --filter @markflow/api test -- tests/integration/comments.test.ts` 가 모든 테스트 통과
2. 7개 describe 블록 (Create, List, Update, Delete, Resolve, Permissions, Threading)이 존재
3. factory.ts에 createDocument, createComment이 export되어 있음
</verification>

<success_criteria>
- TEST-01 요구사항: 댓글 통합 테스트가 생성/조회/수정/삭제/해결/권한/스레딩 7개 시나리오를 검증하고 모두 통과한다
- comments.test.ts 파일에 13개+ 테스트가 존재하며 모두 green
- factory.ts에 createDocument(), createComment() 헬퍼가 추가되어 재사용 가능
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage/02-01-SUMMARY.md`
</output>
