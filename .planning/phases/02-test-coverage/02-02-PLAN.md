---
phase: 02-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/editor/package.json
  - packages/editor/vitest.config.ts
  - packages/editor/src/utils/__tests__/imageValidation.test.ts
  - packages/editor/src/utils/__tests__/cloudflareUploader.test.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "validateImageFile()이 png/jpeg/gif/webp/svg 파일을 허용한다"
    - "validateImageFile()이 지원하지 않는 MIME 타입을 거부한다"
    - "validateImageFile()이 10MB 초과 파일을 거부한다"
    - "validateImageFile()이 0 byte 빈 파일을 거부한다"
    - "createCloudflareUploader()가 성공 응답 시 URL을 반환한다"
    - "createCloudflareUploader()가 non-ok 응답 시 에러를 throw한다"
    - "createCloudflareUploader()가 네트워크 에러 시 에러를 throw한다"
    - "createCloudflareUploader()가 CORS 에러(TypeError) 시 에러를 throw한다"
  artifacts:
    - path: "packages/editor/vitest.config.ts"
      provides: "editor 패키지 vitest 설정"
      contains: "defineConfig"
    - path: "packages/editor/src/utils/__tests__/imageValidation.test.ts"
      provides: "이미지 타입/크기 검증 테스트"
      contains: "validateImageFile"
    - path: "packages/editor/src/utils/__tests__/cloudflareUploader.test.ts"
      provides: "업로드 성공/CORS/에러 처리 테스트"
      contains: "createCloudflareUploader"
  key_links:
    - from: "packages/editor/src/utils/__tests__/imageValidation.test.ts"
      to: "packages/editor/src/utils/imageValidation.ts"
      via: "import { validateImageFile }"
      pattern: "import.*validateImageFile.*imageValidation"
    - from: "packages/editor/src/utils/__tests__/cloudflareUploader.test.ts"
      to: "packages/editor/src/utils/cloudflareUploader.ts"
      via: "import { createCloudflareUploader, createTestImage }"
      pattern: "import.*createCloudflareUploader.*cloudflareUploader"
---

<objective>
이미지 업로드 클라이언트 검증 테스트 5개 시나리오를 작성하여 TEST-02 요구사항을 충족한다.

Purpose: 클라이언트 사이드 이미지 검증 로직(validateImageFile, createCloudflareUploader)이 올바르게 동작함을 단위 테스트로 검증한다 (per D-01).
Output: editor 패키지 vitest 설정 + imageValidation.test.ts + cloudflareUploader.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-coverage/02-CONTEXT.md
@.planning/phases/02-test-coverage/02-RESEARCH.md

<interfaces>
<!-- editor 패키지의 테스트 대상 함수 시그니처 -->

From packages/editor/src/utils/imageValidation.ts:
```typescript
export interface ValidationResult {
  valid: boolean;
  error?: string;
}

const ALLOWED_TYPES = ['image/png', 'image/jpeg', 'image/gif', 'image/webp', 'image/svg+xml'];
const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB

export function validateImageFile(file: File): ValidationResult;
// - !ALLOWED_TYPES.includes(file.type) → { valid: false, error: '지원하지 않는 파일 형식입니다. (png, jpeg, gif, webp, svg+xml)' }
// - file.size > MAX_FILE_SIZE → { valid: false, error: '파일 크기가 너무 큽니다. (${sizeMB}MB / 최대 10MB)' }
// - file.size === 0 → { valid: false, error: '빈 파일은 업로드할 수 없습니다.' }
// - otherwise → { valid: true }
```

From packages/editor/src/utils/cloudflareUploader.ts:
```typescript
interface CloudflareUploadResponse {
  success: boolean;
  url?: string;
  error?: string;
}

export function createCloudflareUploader(workerUrl: string): (file: File) => Promise<string>;
// - workerUrl이 빈 문자열이면 → throw Error('Cloudflare Worker URL is not configured')
// - fetch POST ${baseUrl}/upload with FormData
// - !response.ok → throw Error(`Upload failed (${response.status}): ${errorText}`)
// - !result.success || !result.url → throw Error(result.error || 'Upload failed: invalid response')
// - success → return result.url

export function createTestImage(): File;
// - 1x1 transparent PNG (67 bytes) File 객체 반환
```

From packages/editor/package.json (현재 상태):
```json
{
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
    // "test" 스크립트 없음 — 추가 필요
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tsup": "^8",
    "typescript": "^5"
    // vitest 없음 — 추가 필요
  }
}
```

기존 editor 테스트 (packages/editor/src/utils/__tests__/ 에 존재):
- markdownActions.test.ts
- wordCount.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: editor 패키지에 vitest 설정 추가</name>
  <files>packages/editor/package.json, packages/editor/vitest.config.ts</files>
  <read_first>
    - packages/editor/package.json
    - packages/editor/tsconfig.json
    - apps/api/vitest.config.ts (기존 vitest config 참고)
    - packages/editor/src/utils/__tests__/markdownActions.test.ts (기존 테스트 패턴 확인)
  </read_first>
  <action>
    1. vitest devDependency 설치:
       ```bash
       pnpm --filter @markflow/editor add -D vitest
       ```

    2. `packages/editor/vitest.config.ts` 생성:
       ```typescript
       import { defineConfig } from 'vitest/config';

       export default defineConfig({
         test: {
           globals: false,
           environment: 'node',
         },
       });
       ```
       Note: editor 패키지 테스트는 순수 함수 테스트이므로 `jsdom` 환경 불필요. `File`, `FormData`, `fetch`는 Node.js 20+에서 global로 사용 가능. 만약 File이 없으면 `environment: 'jsdom'` 으로 변경 필요 — 실행 시 확인.

    3. `packages/editor/package.json`의 `scripts`에 추가:
       ```json
       "test": "vitest run",
       "test:watch": "vitest"
       ```
       기존 `"build"`, `"dev"` 스크립트는 유지.
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/editor test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/editor/package.json에 `"test": "vitest run"` 문자열이 존재
    - packages/editor/package.json의 devDependencies에 `"vitest"` 키가 존재
    - packages/editor/vitest.config.ts 파일이 존재
    - packages/editor/vitest.config.ts에 `defineConfig` 문자열이 존재
    - `pnpm --filter @markflow/editor test` 가 exit code 0으로 완료 (기존 markdownActions.test.ts, wordCount.test.ts 포함)
  </acceptance_criteria>
  <done>editor 패키지에 vitest가 설정되어 `pnpm --filter @markflow/editor test`와 `pnpm test` (turbo) 모두에서 테스트가 실행된다. 기존 2개 테스트 파일도 포함.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: imageValidation.test.ts 작성 — 타입 검증 + 크기 제한 (per D-01, D-02 시나리오 1,2)</name>
  <files>packages/editor/src/utils/__tests__/imageValidation.test.ts</files>
  <read_first>
    - packages/editor/src/utils/imageValidation.ts
    - packages/editor/src/utils/__tests__/markdownActions.test.ts (기존 테스트 패턴 참고)
  </read_first>
  <behavior>
    — 타입 검증 시나리오 (TEST-02-a) —
    - Test 1: image/png → { valid: true }
    - Test 2: image/jpeg → { valid: true }
    - Test 3: image/gif → { valid: true }
    - Test 4: image/webp → { valid: true }
    - Test 5: image/svg+xml → { valid: true }
    - Test 6: application/pdf → { valid: false, error 에 '지원하지 않는 파일 형식' 포함 }
    - Test 7: text/plain → { valid: false }

    — 크기 제한 시나리오 (TEST-02-b) —
    - Test 8: 정확히 10MB (10 * 1024 * 1024) → { valid: true }
    - Test 9: 10MB + 1 byte (10 * 1024 * 1024 + 1) → { valid: false, error에 '파일 크기가 너무 큽니다' 포함 }
    - Test 10: 0 byte → { valid: false, error에 '빈 파일' 포함 }
  </behavior>
  <action>
    `packages/editor/src/utils/__tests__/imageValidation.test.ts` 파일을 생성한다.

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { validateImageFile } from '../imageValidation';

    // 헬퍼: 특정 type/size의 File 생성
    function createFile(name: string, type: string, size: number): File {
      const file = new File(['x'], name, { type });
      Object.defineProperty(file, 'size', { value: size, writable: false });
      return file;
    }

    describe('validateImageFile', () => {
      describe('Type validation', () => {
        // image/png, image/jpeg, image/gif, image/webp, image/svg+xml → valid: true
        // application/pdf, text/plain → valid: false, error에 '지원하지 않는 파일 형식' 포함
      });

      describe('Size validation', () => {
        // 10MB 정확 → valid: true
        // 10MB + 1 → valid: false, error에 '파일 크기가 너무 큽니다' 포함
        // 0 byte → valid: false, error에 '빈 파일' 포함
      });
    });
    ```

    각 `it` 블록에서 `createFile()` 헬퍼로 File 객체 생성 후 `validateImageFile()` 호출, 반환값을 `expect()` 로 검증한다.

    **허용 타입 테스트:** 5개 MIME 타입을 `it.each` 또는 개별 `it`으로 검증. size는 1024(1KB)로 설정.
    **거부 타입 테스트:** `application/pdf`, `text/plain`을 개별 `it`으로 검증. `result.valid`이 `false`이고 `result.error`에 `'지원하지 않는 파일 형식'` 문자열이 포함.
    **경계값:** 10 * 1024 * 1024 (10MB 정확히) → valid: true. 10 * 1024 * 1024 + 1 → valid: false.
    **빈 파일:** size 0 → valid: false, error에 `'빈 파일은 업로드할 수 없습니다'` 포함.
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/editor test -- src/utils/__tests__/imageValidation.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/editor/src/utils/__tests__/imageValidation.test.ts 파일이 존재
    - 파일에 `import { validateImageFile }` 문자열이 존재
    - 파일에 `describe('Type validation'` 문자열이 존재
    - 파일에 `describe('Size validation'` 문자열이 존재
    - 파일에 `image/png` 문자열이 존재
    - 파일에 `10 * 1024 * 1024` 또는 `10485760` 패턴이 존재
    - 파일에 `'지원하지 않는 파일 형식'` 문자열이 존재
    - 파일에 `'빈 파일'` 문자열이 존재
    - `pnpm --filter @markflow/editor test -- src/utils/__tests__/imageValidation.test.ts` 가 exit code 0으로 완료
  </acceptance_criteria>
  <done>imageValidation.test.ts가 타입 검증(5개 허용 + 2개 거부) + 크기 제한(경계값 + 빈 파일) 총 10개 테스트를 포함하며 모두 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cloudflareUploader.test.ts 작성 — CORS/성공 업로드/에러 처리 (per D-01, D-02 시나리오 3,4,5)</name>
  <files>packages/editor/src/utils/__tests__/cloudflareUploader.test.ts</files>
  <read_first>
    - packages/editor/src/utils/cloudflareUploader.ts
    - packages/editor/src/utils/__tests__/imageValidation.test.ts (Task 2 결과 패턴 참고)
  </read_first>
  <behavior>
    — CORS 시나리오 (TEST-02-c) —
    - Test 1: fetch가 TypeError throw 시 (CORS 에러 시뮬레이션) → upload 함수가 TypeError를 re-throw

    — 성공 업로드 시나리오 (TEST-02-d) —
    - Test 2: fetch가 ok:true, json { success: true, url: 'https://r2.example.com/img.png' } 반환 → upload 함수가 'https://r2.example.com/img.png' 반환
    - Test 3: fetch가 호출될 때 URL이 `${workerUrl}/upload`이고 method가 'POST'

    — 에러 처리 시나리오 (TEST-02-e) —
    - Test 4: fetch가 ok:false, status:413 반환 → throw Error('Upload failed (413): File too large')
    - Test 5: fetch가 ok:true, json { success: false, error: 'Quota exceeded' } 반환 → throw Error('Quota exceeded')
    - Test 6: fetch가 ok:true, json { success: true } (url 없음) 반환 → throw Error('Upload failed: invalid response')
    - Test 7: workerUrl이 빈 문자열 → throw Error('Cloudflare Worker URL is not configured')
  </behavior>
  <action>
    `packages/editor/src/utils/__tests__/cloudflareUploader.test.ts` 파일을 생성한다.

    ```typescript
    import { describe, it, expect, vi, afterEach } from 'vitest';
    import { createCloudflareUploader, createTestImage } from '../cloudflareUploader';
    ```

    **fetch mock 전략 (Claude's Discretion — vitest vi.fn 사용):**
    - 각 테스트에서 `globalThis.fetch = vi.fn().mockResolvedValue(...)` 또는 `vi.fn().mockRejectedValue(...)` 로 mock
    - `afterEach`에서 `vi.restoreAllMocks()` 호출하고 `globalThis.fetch`를 원본으로 복원

    ```typescript
    const WORKER_URL = 'https://worker.example.com';
    const originalFetch = globalThis.fetch;

    afterEach(() => {
      globalThis.fetch = originalFetch;
      vi.restoreAllMocks();
    });
    ```

    **describe 구조:**
    ```
    describe('createCloudflareUploader', () => {
      describe('Configuration', () => {
        // workerUrl 빈 문자열 → Error
      });

      describe('Successful upload', () => {
        // fetch ok:true, success:true, url 존재 → URL 반환
        // fetch 호출 시 URL과 method 검증
      });

      describe('CORS error', () => {
        // fetch가 TypeError throw → re-throw
      });

      describe('Error handling', () => {
        // non-ok response (413) → Error with status
        // success:false with error → Error with error message
        // success:true but no url → Error 'invalid response'
      });
    });
    ```

    **CORS 에러 시뮬레이션:**
    ```typescript
    globalThis.fetch = vi.fn().mockRejectedValue(new TypeError('Failed to fetch'));
    const upload = createCloudflareUploader(WORKER_URL);
    await expect(upload(createTestImage())).rejects.toThrow('Failed to fetch');
    ```

    **성공 업로드:**
    ```typescript
    globalThis.fetch = vi.fn().mockResolvedValue({
      ok: true,
      json: () => Promise.resolve({ success: true, url: 'https://r2.example.com/img.png' }),
    });
    const upload = createCloudflareUploader(WORKER_URL);
    const url = await upload(createTestImage());
    expect(url).toBe('https://r2.example.com/img.png');
    expect(globalThis.fetch).toHaveBeenCalledWith(
      'https://worker.example.com/upload',
      expect.objectContaining({ method: 'POST' }),
    );
    ```

    **Non-ok 응답:**
    ```typescript
    globalThis.fetch = vi.fn().mockResolvedValue({
      ok: false,
      status: 413,
      text: () => Promise.resolve('File too large'),
    });
    await expect(upload(createTestImage())).rejects.toThrow('Upload failed (413)');
    ```
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/editor test -- src/utils/__tests__/cloudflareUploader.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/editor/src/utils/__tests__/cloudflareUploader.test.ts 파일이 존재
    - 파일에 `import { createCloudflareUploader, createTestImage }` 문자열이 존재
    - 파일에 `globalThis.fetch = vi.fn()` 패턴이 존재
    - 파일에 `TypeError` 문자열이 존재 (CORS 에러 테스트)
    - 파일에 `'Upload failed (413)'` 또는 `Upload failed` 패턴이 존재
    - 파일에 `'https://r2.example.com/img.png'` 문자열이 존재
    - 파일에 `toHaveBeenCalledWith` 패턴이 존재
    - 파일에 `afterEach` 에서 fetch 복원 로직이 존재
    - `pnpm --filter @markflow/editor test -- src/utils/__tests__/cloudflareUploader.test.ts` 가 exit code 0으로 완료
  </acceptance_criteria>
  <done>cloudflareUploader.test.ts가 CORS 에러/성공 업로드/에러 처리 3개 시나리오 7개 테스트를 포함하며 모두 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → fetch mock | 테스트가 globalThis.fetch를 mock으로 교체 — 실제 네트워크 호출 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | cloudflareUploader.test.ts | accept | 테스트에 하드코딩된 URL('https://worker.example.com')은 실제 Worker URL이 아닌 가짜 값. fetch mock으로 실제 네트워크 호출 없음 |
| T-02-06 | Denial of Service | imageValidation.test.ts | accept | Object.defineProperty로 size만 변경, 실제 대용량 파일 생성 없음. 테스트 실행 시간 영향 없음 |
</threat_model>

<verification>
1. `pnpm --filter @markflow/editor test` 가 모든 테스트 통과 (기존 2개 + 새 imageValidation + cloudflareUploader)
2. imageValidation.test.ts에 타입 검증(허용 5 + 거부 2) + 크기 제한(경계값 + 빈 파일) 테스트 존재
3. cloudflareUploader.test.ts에 CORS/성공/에러 처리 테스트 존재
4. `pnpm test` (turbo 전체) 가 green
</verification>

<success_criteria>
- TEST-02 요구사항: 이미지 업로드 테스트가 타입 검증/크기 제한/CORS/성공 업로드/에러 처리 5개 시나리오를 검증하고 모두 통과한다
- editor 패키지에 vitest가 설정되어 turbo 파이프라인에 포함됨
- imageValidation.test.ts에 10개 테스트, cloudflareUploader.test.ts에 7개 테스트가 존재하며 모두 green
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage/02-02-SUMMARY.md`
</output>
