---
phase: 03-category-document-context-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/category-service.ts
  - apps/api/src/routes/categories.ts
  - apps/api/tests/integration/category-context.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
must_haves:
  truths:
    - "GET /workspaces/:wsId/categories/:id/ancestors returns all ancestors from root to leaf order"
    - "GET /workspaces/:wsId/categories/:id/ancestors returns empty array for root category"
    - "GET /workspaces/:wsId/categories/:id/ancestors returns 404 for non-existent category"
    - "GET /workspaces/:wsId/categories/:id/descendants returns nested tree with documents"
    - "GET /workspaces/:wsId/categories/:id/descendants returns empty children for leaf category"
    - "Ancestors and descendants are workspace-isolated (other workspace data never leaks)"
  artifacts:
    - path: "apps/api/src/services/category-service.ts"
      provides: "ancestors() and descendants() methods"
      contains: "async function ancestors"
    - path: "apps/api/src/routes/categories.ts"
      provides: "GET /:id/ancestors and GET /:id/descendants endpoints"
      contains: "/ancestors"
    - path: "apps/api/tests/integration/category-context.test.ts"
      provides: "Integration tests for ancestors and descendants"
      contains: "describe"
  key_links:
    - from: "apps/api/src/routes/categories.ts"
      to: "apps/api/src/services/category-service.ts"
      via: "categoryService.ancestors() and categoryService.descendants()"
      pattern: "categoryService\\.ancestors|categoryService\\.descendants"
    - from: "apps/api/src/services/category-service.ts"
      to: "@markflow/db categoryClosure table"
      via: "Drizzle query with eq(categoryClosure.descendantId) and eq(categoryClosure.ancestorId)"
      pattern: "categoryClosure"
---

<objective>
카테고리 ancestors/descendants API를 구현하여 프론트엔드가 breadcrumb과 하위 트리를 단일 호출로 렌더링할 수 있게 한다.

Purpose: 기존 categoryClosure 테이블 인프라를 read-only API로 노출. 새 라이브러리 없이 기존 서비스 확장만으로 구현.
Output: ancestors/descendants 서비스 메서드 + Fastify 라우트 + 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-category-document-context-api/03-CONTEXT.md
@.planning/phases/03-category-document-context-api/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/db/src/schema/categories.ts:
```typescript
export const categories = pgTable('categories', {
  id: bigserial('id', { mode: 'number' }).primaryKey(),
  workspaceId: bigint('workspace_id', { mode: 'number' }).notNull(),
  name: varchar('name', { length: 100 }).notNull(),
  parentId: bigint('parent_id', { mode: 'number' }),
  orderIndex: doublePrecision('order_index').notNull().default(0),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
});

export const categoryClosure = pgTable('category_closure', {
  ancestorId: bigint('ancestor_id', { mode: 'number' }).notNull(),
  descendantId: bigint('descendant_id', { mode: 'number' }).notNull(),
  depth: integer('depth').notNull(),
});
```

From apps/api/src/services/category-service.ts:
```typescript
export interface CategoryTreeDocument {
  id: number;
  title: string;
  updatedAt: Date;
}

export interface CategoryTreeNode {
  id: number;
  name: string;
  parentId: number | null;
  children: CategoryTreeNode[];
  documents: CategoryTreeDocument[];
}

export function createCategoryService(db: Db) {
  // Returns: { create, list, tree, rename, remove, reorder }
  // ancestors() and descendants() will be added here
}
```

From packages/db/src/index.ts (available exports):
```typescript
export { categories, categoryClosure } from './schema/categories';
export { documents } from './schema/documents';
export { eq, and, or, desc, asc, sql, isNull, gt, lt, gte, lte, ne, like, ilike, inArray, notInArray, count } from 'drizzle-orm';
```

From apps/api/src/routes/categories.ts (route pattern):
```typescript
export async function categoriesRoutes(app: FastifyInstance, opts: CategoriesRoutesOptions) {
  const categoryService = createCategoryService(opts.db);
  app.addHook('preHandler', authMiddleware);
  // Existing: POST, GET list, GET tree, PATCH, PUT reorder, DELETE
  // New ancestors/descendants endpoints will follow same pattern
}
```

From apps/api/tests/helpers/factory.ts:
```typescript
export async function createUser(db: Db, options?: CreateUserOptions): Promise<{ user, accessToken }>;
export async function createWorkspace(db: Db, ownerId: number, options?: CreateWorkspaceOptions): Promise<Workspace>;
export async function addMember(db: Db, workspaceId: number, userId: number, role: string): Promise<Member>;
```

From apps/api/tests/helpers/setup.ts:
```typescript
export function getApp(): FastifyInstance;
export function getDb(): Db;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ancestors() 서비스 메서드 + 라우트 + 테스트</name>
  <files>
    apps/api/src/services/category-service.ts,
    apps/api/src/routes/categories.ts,
    apps/api/tests/integration/category-context.test.ts
  </files>
  <read_first>
    apps/api/src/services/category-service.ts,
    apps/api/src/routes/categories.ts,
    packages/db/src/schema/categories.ts,
    apps/api/tests/integration/categories.test.ts,
    apps/api/tests/helpers/factory.ts,
    apps/api/tests/helpers/setup.ts
  </read_first>
  <behavior>
    - Test 1: 3-depth 카테고리 체인(Root > Mid > Leaf)에서 Leaf의 ancestors 호출 시 [Root, Mid] 순서로 반환 (Root→Leaf 순, D-01)
    - Test 2: 각 ancestor 객체에 id, name, parentId, depth, createdAt 포함 (D-02)
    - Test 3: 루트 카테고리의 ancestors 호출 시 빈 배열 반환
    - Test 4: 존재하지 않는 카테고리 ID로 호출 시 404
    - Test 5: 다른 워크스페이스의 카테고리 ID로 호출 시 404 (워크스페이스 격리)
  </behavior>
  <action>
    **TDD RED phase** -- 테스트 먼저 작성:

    1. `apps/api/tests/integration/category-context.test.ts` 파일 생성. import 패턴은 기존 `categories.test.ts`와 동일:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { getApp, getDb } from '../helpers/setup.js';
       import { createUser, createWorkspace } from '../helpers/factory.js';
       ```

    2. 카테고리 생성 헬퍼: 테스트 내에서 `app.inject({ method: 'POST', url: /api/v1/workspaces/${wsId}/categories, payload: { name, parentId } })` 사용하여 카테고리 생성 (기존 categories.test.ts 패턴과 동일).

    3. `describe('GET /api/v1/workspaces/:wsId/categories/:id/ancestors')` 블록에 위 5개 테스트 작성.

    4. 응답 타입 인터페이스:
       ```typescript
       interface AncestorResponse {
         id: number;
         name: string;
         parentId: number | null;
         depth: number;
         createdAt: string;
       }
       ```

    **TDD GREEN phase** -- 구현:

    5. `apps/api/src/services/category-service.ts`에 `ancestors()` 함수 추가:
       - 파라미터: `(categoryId: string, workspaceId: string)`
       - 카테고리 존재/워크스페이스 소속 확인: `db.select({ id: categories.id }).from(categories).where(and(eq(categories.id, Number(categoryId)), eq(categories.workspaceId, Number(workspaceId)))).limit(1)` -- 없으면 `throw notFound('Category not found')`
       - Closure table 직접 조회: `db.select({ id: categories.id, name: categories.name, parentId: categories.parentId, depth: categoryClosure.depth, createdAt: categories.createdAt }).from(categoryClosure).innerJoin(categories, eq(categoryClosure.ancestorId, categories.id)).where(and(eq(categoryClosure.descendantId, Number(categoryId)), gt(categoryClosure.depth, 0))).orderBy(desc(categoryClosure.depth))` -- depth DESC로 Root first (D-01)
       - 필요한 추가 import: `gt, desc` from `@markflow/db` (이미 re-export 됨)

    6. `return` 객체에 `ancestors` 추가: `return { create, list, tree, rename, remove, reorder, ancestors };`

    7. `apps/api/src/routes/categories.ts`에 GET 라우트 추가:
       ```typescript
       app.get<{
         Params: { wsId: string; id: string };
       }>('/workspaces/:wsId/categories/:id/ancestors', {
         preHandler: requireRole('viewer'),  // D-09
       }, async (request, reply) => {
         const ancestors = await categoryService.ancestors(
           request.params.id,
           request.params.wsId,
         );
         return reply.status(200).send({ ancestors });
       });
       ```
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/api test -- --run tests/integration/category-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - category-service.ts contains `async function ancestors(categoryId: string, workspaceId: string)`
    - category-service.ts contains `gt(categoryClosure.depth, 0)` (self-reference 제외)
    - category-service.ts contains `desc(categoryClosure.depth)` (Root→Leaf 정렬, D-01)
    - category-service.ts return object contains `ancestors`
    - categories.ts contains `/workspaces/:wsId/categories/:id/ancestors`
    - categories.ts contains `requireRole('viewer')` for ancestors route (D-09)
    - category-context.test.ts contains at least 5 test cases (`it(` 5회 이상)
    - category-context.test.ts exits 0
  </acceptance_criteria>
  <done>ancestors API가 Root→Leaf 순서로 풀 카테고리 객체를 반환하고, 루트 카테고리는 빈 배열, 존재하지 않는/다른 워크스페이스 카테고리는 404 반환. 5개 테스트 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: descendants() 서비스 메서드 + 라우트 + 테스트</name>
  <files>
    apps/api/src/services/category-service.ts,
    apps/api/src/routes/categories.ts,
    apps/api/tests/integration/category-context.test.ts
  </files>
  <read_first>
    apps/api/src/services/category-service.ts,
    apps/api/src/routes/categories.ts,
    packages/db/src/schema/categories.ts,
    packages/db/src/schema/documents.ts
  </read_first>
  <behavior>
    - Test 1: 3-depth 체인(Root > Mid > Leaf)에서 Root의 descendants 호출 시 nested tree 구조 반환 (D-03). Root의 children에 Mid, Mid의 children에 Leaf.
    - Test 2: 각 카테고리 노드에 documents 배열 포함 (D-04). 문서가 있는 카테고리는 {id, title, updatedAt} 포함.
    - Test 3: 리프 카테고리의 descendants 호출 시 children 빈 배열
    - Test 4: 삭제된 문서(isDeleted=true)는 descendants documents에 포함되지 않음
    - Test 5: 다른 워크스페이스의 카테고리 ID로 호출 시 404
  </behavior>
  <action>
    **TDD RED phase** -- `category-context.test.ts`에 descendants 테스트 추가:

    1. `describe('GET /api/v1/workspaces/:wsId/categories/:id/descendants')` 블록 추가.

    2. 응답 타입:
       ```typescript
       interface DescendantTreeNode {
         id: number;
         name: string;
         parentId: number | null;
         children: DescendantTreeNode[];
         documents: Array<{ id: number; title: string; updatedAt: string }>;
       }
       ```

    3. 테스트 시나리오에서 카테고리 3개 (Root > Mid > Leaf) + 문서 2개 (Mid에 1개, Leaf에 1개) 생성 후 Root의 descendants 호출.

    4. 삭제된 문서 테스트: 문서를 soft-delete한 후 descendants에서 제외되는지 확인. soft-delete는 `app.inject({ method: 'DELETE', url: /api/v1/workspaces/${wsId}/documents/${docId} })` 사용 (trash로 이동).

    **TDD GREEN phase** -- 구현:

    5. `apps/api/src/services/category-service.ts`에 `descendants()` 함수 추가:
       - 파라미터: `(categoryId: string, workspaceId: string)`
       - 카테고리 존재 확인 (ancestors와 동일 패턴)
       - Step 1: closure에서 descendant ID 목록: `db.select({ descendantId: categoryClosure.descendantId }).from(categoryClosure).where(and(eq(categoryClosure.ancestorId, Number(categoryId)), gt(categoryClosure.depth, 0)))`
       - 빈 결과 시 `return { children: [], documents: [] }` 반환
       - Step 2: 카테고리 상세 조회 (워크스페이스 검증): `db.select({ id, name, parentId }).from(categories).where(and(inArray(categories.id, descendantIds), eq(categories.workspaceId, Number(workspaceId)))).orderBy(asc(categories.orderIndex), asc(categories.name))`
       - Step 3: 문서 조회 (D-04): `db.select({ id, title, categoryId, updatedAt }).from(documents).where(and(inArray(documents.categoryId, descendantIds), eq(documents.workspaceId, Number(workspaceId)), eq(documents.isDeleted, false)))`
       - Step 4: 기존 `tree()` 메서드의 nodeMap 패턴과 동일하게 트리 조립. `CategoryTreeNode` 타입 재사용 (D-03).
       - 필요한 추가 import: `inArray` (이미 사용 중인지 확인, 없으면 추가)
       - 추가 import: `documents` from `@markflow/db` (이미 import 중)

    6. `return` 객체에 `descendants` 추가.

    7. `apps/api/src/routes/categories.ts`에 GET 라우트 추가:
       ```typescript
       app.get<{
         Params: { wsId: string; id: string };
       }>('/workspaces/:wsId/categories/:id/descendants', {
         preHandler: requireRole('viewer'),  // D-09
       }, async (request, reply) => {
         const result = await categoryService.descendants(
           request.params.id,
           request.params.wsId,
         );
         return reply.status(200).send(result);
       });
       ```
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/api test -- --run tests/integration/category-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - category-service.ts contains `async function descendants(categoryId: string, workspaceId: string)`
    - category-service.ts contains `eq(categoryClosure.ancestorId, Number(categoryId))`
    - category-service.ts contains `eq(documents.isDeleted, false)` in descendants query
    - category-service.ts return object contains `descendants`
    - categories.ts contains `/workspaces/:wsId/categories/:id/descendants`
    - categories.ts contains `requireRole('viewer')` for descendants route (D-09)
    - category-context.test.ts contains `describe('GET /api/v1/workspaces/:wsId/categories/:id/descendants'`
    - category-context.test.ts exits 0 (all tests including both ancestors and descendants pass)
  </acceptance_criteria>
  <done>descendants API가 nested tree 구조(CategoryTreeNode)에 문서 목록을 포함하여 반환. 삭제된 문서 제외, 워크스페이스 격리 확인. ancestors + descendants 전체 테스트 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | 인증된 사용자가 자신이 속하지 않은 워크스페이스의 카테고리 데이터 접근 시도 |
| categoryClosure -> categories | closure table JOIN 시 다른 워크스페이스의 카테고리가 포함될 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ancestors/descendants | mitigate | 모든 categories 쿼리에 `eq(categories.workspaceId, Number(workspaceId))` 필터 적용. 카테고리 존재 확인 시에도 workspaceId 조건 포함. |
| T-03-02 | Information Disclosure | descendants documents | mitigate | documents 쿼리에 `eq(documents.isDeleted, false)` 및 `eq(documents.workspaceId, Number(workspaceId))` 필터 적용 |
| T-03-03 | Denial of Service | descendants (deep tree) | accept | MVP 10팀 규모에서 카테고리 depth/count 제한 없이도 안전. 향후 필요 시 maxDepth 쿼리 파라미터 추가. |
| T-03-04 | Spoofing | ancestors/descendants route | mitigate | `authMiddleware` hook + `requireRole('viewer')` preHandler로 인증/인가 검증 (기존 패턴 재사용) |
</threat_model>

<verification>
1. `pnpm --filter @markflow/api test -- --run tests/integration/category-context.test.ts` 전체 통과
2. ancestors: 3-depth 체인에서 Root→Leaf 순서 반환 확인
3. descendants: nested tree + documents 포함 확인
4. 워크스페이스 격리 테스트 통과
5. 기존 categories 테스트 회귀 없음: `pnpm --filter @markflow/api test -- --run tests/integration/categories.test.ts`
</verification>

<success_criteria>
- ancestors API가 closure table에서 depth DESC 정렬로 Root→Leaf 순서 조상 반환
- descendants API가 nodeMap 패턴으로 nested tree + documents 반환
- 10개 이상 테스트 케이스 통과 (ancestors 5 + descendants 5)
- 기존 카테고리 테스트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/03-category-document-context-api/03-01-SUMMARY.md`
</output>
