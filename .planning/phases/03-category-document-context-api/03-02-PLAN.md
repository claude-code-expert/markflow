---
phase: 03-category-document-context-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/graph-service.ts
  - apps/api/src/routes/graph.ts
  - apps/api/tests/integration/dag-context.test.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "GET /workspaces/:wsId/graph/documents/:id/context returns incoming, outgoing, and related relations"
    - "Each related document includes title, categoryId, categoryName, and tags"
    - "Soft-deleted documents are excluded from DAG context"
    - "DAG context is workspace-isolated"
    - "Non-existent or other-workspace document returns 404"
  artifacts:
    - path: "apps/api/src/services/graph-service.ts"
      provides: "getDocumentContext() method"
      contains: "async function getDocumentContext"
    - path: "apps/api/src/routes/graph.ts"
      provides: "GET /documents/:id/context endpoint"
      contains: "/documents/:id/context"
    - path: "apps/api/tests/integration/dag-context.test.ts"
      provides: "Integration tests for DAG context"
      contains: "describe"
  key_links:
    - from: "apps/api/src/routes/graph.ts"
      to: "apps/api/src/services/graph-service.ts"
      via: "graphService.getDocumentContext()"
      pattern: "graphService\\.getDocumentContext"
    - from: "apps/api/src/services/graph-service.ts"
      to: "@markflow/db documentRelations + documents + categories + tags"
      via: "Drizzle JOIN queries for incoming/outgoing relations + metadata"
      pattern: "documentRelations|documentTags"
---

<objective>
문서 DAG context API를 구현하여 프론트엔드가 단일 호출로 문서의 전후방 관계와 관련 문서 메타데이터를 가져올 수 있게 한다.

Purpose: 기존 documentRelations 테이블을 양방향(incoming/outgoing/related) read-only API로 노출. 관련 문서의 제목, 카테고리, 태그를 JOIN으로 한 번에 제공.
Output: getDocumentContext 서비스 메서드 + Fastify 라우트 + 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-category-document-context-api/03-CONTEXT.md
@.planning/phases/03-category-document-context-api/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/db/src/schema/document-relations.ts:
```typescript
export const documentRelations = pgTable('document_relations', {
  id: bigserial('id', { mode: 'number' }).primaryKey(),
  sourceId: bigint('source_id', { mode: 'number' }).notNull(),
  targetId: bigint('target_id', { mode: 'number' }).notNull(),
  type: varchar('type', { length: 20 }).$type<'prev' | 'next' | 'related'>().notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
});
```

From packages/db/src/schema/tags.ts:
```typescript
export const tags = pgTable('tags', {
  id: bigserial('id', { mode: 'number' }).primaryKey(),
  workspaceId: bigint('workspace_id', { mode: 'number' }).notNull(),
  name: varchar('name', { length: 50 }).notNull(),
});

export const documentTags = pgTable('document_tags', {
  documentId: bigint('document_id', { mode: 'number' }).notNull(),
  tagId: bigint('tag_id', { mode: 'number' }).notNull(),
});
```

From apps/api/src/services/graph-service.ts:
```typescript
export function createGraphService(db: Db) {
  async function getWorkspaceGraph(workspaceId: string): Promise<WorkspaceGraph> { ... }
  // getDocumentContext() will be added here
  return { getWorkspaceGraph };
}
```

From apps/api/src/services/relation-service.ts (existing pattern for JOIN):
```typescript
// getRelations() uses sourceId-only query with innerJoin(documents).
// DAG context needs BOTH sourceId and targetId queries (incoming + outgoing).
async function getRelations(docId: string): Promise<RelationsResult> {
  const rows = await db
    .select({ type, targetId, docId, docTitle })
    .from(documentRelations)
    .innerJoin(documents, and(eq(documentRelations.targetId, documents.id), eq(documents.isDeleted, false)))
    .where(eq(documentRelations.sourceId, Number(docId)));
}
```

From apps/api/src/routes/graph.ts:
```typescript
export async function graphRoutes(app: FastifyInstance, opts: GraphRoutesOptions) {
  const graphService = createGraphService(opts.db);
  app.addHook('preHandler', authMiddleware);
  // Existing: GET /workspaces/:wsId/graph
  // New: GET /workspaces/:wsId/graph/documents/:id/context
}
```

From packages/db/src/index.ts (available exports):
```typescript
export { documentRelations } from './schema/document-relations';
export { tags, documentTags } from './schema/tags';
export { categories } from './schema/categories';
export { documents } from './schema/documents';
export { eq, and, or, desc, asc, sql, isNull, gt, lt, gte, lte, ne, like, ilike, inArray, notInArray, count } from 'drizzle-orm';
```

From apps/api/tests/helpers (test patterns):
```typescript
// graph.test.ts uses local createDocument helper + app.inject for relation setup
// relations are set via: PUT /api/v1/workspaces/:wsId/documents/:docId/relations { prev, next, related }
// tags are set via: PUT /api/v1/workspaces/:wsId/documents/:docId/tags { tagIds: [...] } or POST /api/v1/workspaces/:wsId/tags { name }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getDocumentContext() 서비스 + 라우트 + 테스트</name>
  <files>
    apps/api/src/services/graph-service.ts,
    apps/api/src/routes/graph.ts,
    apps/api/tests/integration/dag-context.test.ts
  </files>
  <read_first>
    apps/api/src/services/graph-service.ts,
    apps/api/src/services/relation-service.ts,
    apps/api/src/routes/graph.ts,
    packages/db/src/schema/document-relations.ts,
    packages/db/src/schema/tags.ts,
    packages/db/src/schema/documents.ts,
    apps/api/tests/integration/graph.test.ts,
    apps/api/tests/integration/relations.test.ts,
    apps/api/tests/helpers/factory.ts,
    apps/api/tests/helpers/setup.ts
  </read_first>
  <behavior>
    - Test 1: 문서 A가 B를 next로, C를 related로 참조 + D가 A를 related로 참조 -- A의 DAG context에서 outgoing=[B(next), C(related)], incoming=[D(related)] 반환 (D-06)
    - Test 2: 각 관련 문서에 title, categoryId, categoryName, tags 포함 (D-05). 카테고리에 속한 문서는 categoryName이 문자열, 미분류 문서는 categoryId=null/categoryName=null.
    - Test 3: soft-deleted 문서(isDeleted=true)는 outgoing/incoming에서 제외
    - Test 4: 다른 워크스페이스의 문서 ID로 호출 시 404 (워크스페이스 격리)
    - Test 5: 관계 없는 문서의 DAG context 호출 시 incoming=[], outgoing=[], related=[] 반환
    - Test 6: 존재하지 않는 문서 ID로 호출 시 404
    - Test 7: related 타입은 양방향 수집 후 중복 제거 -- A가 B를 related로 설정하면 A context의 outgoing에 B(related), B context의 incoming에 A(related)
  </behavior>
  <action>
    **TDD RED phase** -- 테스트 먼저:

    1. `apps/api/tests/integration/dag-context.test.ts` 파일 생성. import 패턴:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { getApp, getDb } from '../helpers/setup.js';
       import { createUser, createWorkspace, addMember } from '../helpers/factory.js';
       ```

    2. 문서 생성 헬퍼 (graph.test.ts 패턴 재사용):
       ```typescript
       async function createDoc(app, wsId: number, accessToken: string, title: string) {
         const res = await app.inject({
           method: 'POST',
           url: `/api/v1/workspaces/${wsId}/documents`,
           headers: { authorization: `Bearer ${accessToken}` },
           payload: { title },
         });
         expect(res.statusCode).toBe(201);
         return (res.json() as { document: { id: number; title: string } }).document;
       }
       ```

    3. 관계 설정 헬퍼: `PUT /api/v1/workspaces/:wsId/documents/:docId/relations` 사용. 태그 설정: `POST /api/v1/workspaces/:wsId/tags` + `PUT /api/v1/workspaces/:wsId/documents/:docId/tags`.

    4. DAG context 응답 타입:
       ```typescript
       interface ContextRelation {
         type: string;
         document: {
           id: number;
           title: string;
           categoryId: number | null;
           categoryName: string | null;
           tags: string[];
         };
       }
       interface DagContextResponse {
         incoming: ContextRelation[];
         outgoing: ContextRelation[];
       }
       ```

    5. `describe('GET /api/v1/workspaces/:wsId/graph/documents/:id/context')` 블록에 7개 테스트 작성.

    **TDD GREEN phase** -- 구현:

    6. `apps/api/src/services/graph-service.ts`에 추가 import:
       ```typescript
       import {
         documentRelations,
         documents,
         categories,
         tags,
         documentTags,
         eq,
         and,
         inArray,
       } from '@markflow/db';
       import { notFound } from '../utils/errors.js';
       ```

    7. `getDocumentContext(docId: string, workspaceId: string)` 함수 구현:

       **Step 1 -- 문서 존재 확인:**
       ```typescript
       const numDocId = Number(docId);
       const numWorkspaceId = Number(workspaceId);
       const [doc] = await db
         .select({ id: documents.id })
         .from(documents)
         .where(and(
           eq(documents.id, numDocId),
           eq(documents.workspaceId, numWorkspaceId),
           eq(documents.isDeleted, false),
         ))
         .limit(1);
       if (!doc) throw notFound('Document not found');
       ```

       **Step 2 -- Outgoing 관계 (이 문서가 source):**
       ```typescript
       const outgoingRows = await db
         .select({
           type: documentRelations.type,
           docId: documents.id,
           docTitle: documents.title,
           categoryId: documents.categoryId,
           categoryName: categories.name,
         })
         .from(documentRelations)
         .innerJoin(documents, and(
           eq(documentRelations.targetId, documents.id),
           eq(documents.isDeleted, false),
         ))
         .leftJoin(categories, eq(documents.categoryId, categories.id))
         .where(eq(documentRelations.sourceId, numDocId));
       ```

       **Step 3 -- Incoming 관계 (이 문서가 target):**
       ```typescript
       const incomingRows = await db
         .select({
           type: documentRelations.type,
           docId: documents.id,
           docTitle: documents.title,
           categoryId: documents.categoryId,
           categoryName: categories.name,
         })
         .from(documentRelations)
         .innerJoin(documents, and(
           eq(documentRelations.sourceId, documents.id),
           eq(documents.isDeleted, false),
         ))
         .leftJoin(categories, eq(documents.categoryId, categories.id))
         .where(eq(documentRelations.targetId, numDocId));
       ```

       **Step 4 -- Tags 배치 조회 (N+1 방지):**
       ```typescript
       const relatedDocIds = [...new Set([
         ...outgoingRows.map(r => r.docId),
         ...incomingRows.map(r => r.docId),
       ])];

       const tagMap = new Map<number, string[]>();
       if (relatedDocIds.length > 0) {
         const tagRows = await db
           .select({
             documentId: documentTags.documentId,
             tagName: tags.name,
           })
           .from(documentTags)
           .innerJoin(tags, eq(documentTags.tagId, tags.id))
           .where(inArray(documentTags.documentId, relatedDocIds));

         for (const row of tagRows) {
           const existing = tagMap.get(row.documentId) ?? [];
           existing.push(row.tagName);
           tagMap.set(row.documentId, existing);
         }
       }
       ```

       **Step 5 -- 결과 조립:**
       ```typescript
       function toContextRelation(row: { type: string; docId: number; docTitle: string; categoryId: number | null; categoryName: string | null }) {
         return {
           type: row.type,
           document: {
             id: row.docId,
             title: row.docTitle,
             categoryId: row.categoryId,
             categoryName: row.categoryName,
             tags: tagMap.get(row.docId) ?? [],
           },
         };
       }

       return {
         incoming: incomingRows.map(toContextRelation),
         outgoing: outgoingRows.map(toContextRelation),
       };
       ```

    8. `return` 객체에 `getDocumentContext` 추가: `return { getWorkspaceGraph, getDocumentContext };`

    9. `apps/api/src/routes/graph.ts`에 라우트 추가:
       ```typescript
       app.get<{
         Params: { wsId: string; id: string };
       }>('/workspaces/:wsId/graph/documents/:id/context', {
         preHandler: requireRole('viewer'),  // D-09
       }, async (request, reply) => {
         const context = await graphService.getDocumentContext(
           request.params.id,
           request.params.wsId,
         );
         return reply.status(200).send(context);
       });
       ```
  </action>
  <verify>
    <automated>cd /Users/edell/Project/AI-Project/makrflow && pnpm --filter @markflow/api test -- --run tests/integration/dag-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - graph-service.ts contains `async function getDocumentContext(docId: string, workspaceId: string)`
    - graph-service.ts contains `eq(documentRelations.sourceId, numDocId)` (outgoing query)
    - graph-service.ts contains `eq(documentRelations.targetId, numDocId)` (incoming query)
    - graph-service.ts contains `eq(documents.isDeleted, false)` in both incoming and outgoing JOIN conditions
    - graph-service.ts contains `leftJoin(categories` for categoryName
    - graph-service.ts contains `documentTags` for tags batch query
    - graph-service.ts contains `inArray(documentTags.documentId, relatedDocIds)` for N+1 prevention
    - graph-service.ts return object contains `getDocumentContext`
    - graph.ts contains `/workspaces/:wsId/graph/documents/:id/context`
    - graph.ts contains `requireRole('viewer')` for context route (D-09)
    - dag-context.test.ts contains at least 6 test cases (`it(` 6회 이상)
    - dag-context.test.ts exits 0
  </acceptance_criteria>
  <done>DAG context API가 incoming/outgoing 관계를 3분류로 반환하고, 각 관련 문서에 title + categoryId + categoryName + tags 포함. 삭제된 문서 제외, 워크스페이스 격리 확인. 7개 테스트 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | 인증된 사용자가 다른 워크스페이스의 문서 관계 데이터 접근 시도 |
| documentRelations -> documents | relation이 가리키는 문서가 soft-deleted 상태일 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | getDocumentContext incoming/outgoing | mitigate | documents JOIN 시 `eq(documents.isDeleted, false)` 조건으로 삭제된 문서 메타데이터 노출 차단 |
| T-03-06 | Information Disclosure | getDocumentContext workspace isolation | mitigate | 문서 존재 확인에 `eq(documents.workspaceId, Number(workspaceId))` 필터 적용. 다른 워크스페이스 문서는 404. |
| T-03-07 | Spoofing | graph context route | mitigate | `authMiddleware` hook + `requireRole('viewer')` preHandler로 인증/인가 검증 (기존 패턴 재사용) |
| T-03-08 | Denial of Service | tags batch query | accept | 관련 문서가 많을 경우 inArray 쿼리가 느려질 수 있으나, MVP 10팀 규모에서 단일 문서의 관계 수는 제한적 (related 최대 20개 제한 from relation-service). |
</threat_model>

<verification>
1. `pnpm --filter @markflow/api test -- --run tests/integration/dag-context.test.ts` 전체 통과
2. incoming/outgoing/related 3분류 확인
3. 관련 문서 메타에 title + categoryName + tags 포함 확인
4. 삭제된 문서 제외 확인
5. 워크스페이스 격리 테스트 통과
6. 기존 graph 테스트 회귀 없음: `pnpm --filter @markflow/api test -- --run tests/integration/graph.test.ts`
</verification>

<success_criteria>
- DAG context API가 incoming/outgoing 관계를 분류하여 관련 문서 메타데이터(title, categoryId, categoryName, tags) 포함 반환
- soft-deleted 문서가 관계에서 제외됨
- 워크스페이스 격리 보장
- 7개 이상 테스트 케이스 통과
- 기존 graph 테스트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/03-category-document-context-api/03-02-SUMMARY.md`
</output>
